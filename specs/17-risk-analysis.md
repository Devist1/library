# 17 风险分析（Risk Analysis）

- **输入**：`00-project-brief.md`、`01-clarifying-questions.md`（`R-1`~`R-19` 已关闭）、`02-requirements.md`（v1.1）、`03-use-cases.md`（v1.0）、`04-use-case-model.puml`、`05-domain-model.md`（v1.1）、`06-domain-class-diagram.puml`、`07-architecture.md`（v1.0）、`08-package-diagram.puml`、`13-database-design.md`（v1.0.1）、`19-ai-usage-log.md`、`constitution.md`（v1.0）
- **版本**：v1.0.2
- **日期**：2026-09-24
- **状态**：**草稿，待人工审查**（依 `constitution.md` 第四条：未经审查的产出不得作为下游输入）
- **本次修订**：登记人类裁决 2 项 —— ① **保留四个学生子类**（`H-2` 退化不采纳）；② **配置化边界选项 A**（仅借阅与罚款规则入配置表，其余为需求常量 + 配置项 / 单一入口）。据此关闭 `RISK-MOD-02` 与 `RISK-REQ-12`（均降为低）、`RISK-MOD-01` 降为中，并新增 §12 人类裁决记录（含两条裁决的实现约束与待同步项）
- **编号约定**：`RISK-REQ-x` 需求风险 / `RISK-MOD-x` 建模风险 / `RISK-ARC-x` 架构风险 / `RISK-DB-x` 数据库风险 / `RISK-SEC-x` 权限风险 / `RISK-AGT-x` Agent 使用风险；等级 **高 / 中 / 低**；状态 **开放（须人类裁决）** / **已缓解（有默认处理或设计对策）** / **待验证（须在实现或测试中确认）**
- **约束**：本文件受 `constitution.md` 约束；只**登记**风险与缺口，不替代各上游文档的结论，不擅自裁决 `TBD-xxx` / `H-x` / `ASM-x` / `DB-ASM-x`
- **留痕**：本次由 Agent 生成，依 NFR-007 / 第四条第 5 款须在 `19-ai-usage-log.md` 登记一条记录；本轮人类指令限定**仅创建 `17` 与 `18`**，故登记条目**尚未写入**（见 §10 `RISK-AGT-01`）

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 文档编号 | `17-risk-analysis.md` |
| 上游基线 | `00` / `01`（冻结）、`02`~`08`、`13`、`19`、`constitution.md` |
| 下游影响 | `09-design-model.md`、`14-api-spec.md`、`15-test-plan.md`、`16-tasks.md`、`18-review-checklist.md`、`src/` 实现与测试 |
| 风险总数 | **85 项**（需求 16 / 建模 14 / 架构 15 / 数据库 15 / 权限 13 / Agent 使用 12） |
| 高风险项 | 14 项（见 §9；另有 1 项已裁决降为中、仍列入 §9 待验证） |
| 登记义务 | 第四条第 4 款：假设与缺口**必须**登记本文件，**禁止**只写在对话中；第十一条第 3 款：风险与缺口**必须**登记 `17` |
| 同步要求 | `02` §7 `TBD`、`03` §7.1 `H-1`~`H-5`、`05` §10 `H-1`~`H-4`、`07` §14 `ASM-01`~`ASM-08`、`13` §10 `DB-ASM-01`~`DB-ASM-10` 均须与本文件逐条对应；上游关闭一项，本文件须同轮更新 |

---

## 2. 登记约定

### 2.1 风险来源（本文件的四类登记对象）

| 来源 | 说明 | 本文件处理 |
|---|---|---|
| 上游显式登记的待确认项 | `02` `TBD-002`~`TBD-012`、`03` `H-1`~`H-5`、`05` `H-1`~`H-4` 与附录 A 遗留风险、`07` `ASM-01`~`ASM-08`、`13` `DB-ASM-01`~`DB-ASM-10` | 逐条转登，标注编号与等级 |
| 工件间口径张力 | 图与文档、文档与文档不一致（如 `04` 参与者泛化 vs `03` §2.2） | 单列风险项并给出裁决建议 |
| 尚未生成的工件 | `09`~`12`、`14`~`16` 为 0 字节占位 | 登记为「文档链缺口」风险 |
| Agent 使用过程 | 留痕缺口、默认处理被误当确认、未审查产出被当下游输入 | 单列 §8 |

### 2.2 等级判定口径

| 等级 | 判定 | 处置 |
|---|---|---|
| **高** | 会导致交付物与 `constitution.md` 冲突、核心链路不可运行、或验收不可判定 | 交付前**必须**关闭或由人类明确裁决 |
| **中** | 影响局部实现口径、一致性或可追溯性，可在实现期按默认处理推进 | 建议在 `09` / `16` 成文前裁决 |
| **低** | 影响有限、已有明确默认处理或仅涉及命名与格式 | 可在实现期顺带收口 |

---

## 3. 需求风险（`RISK-REQ-x`）

| 编号 | 风险 | 来源 / 触发点 | 潜在影响 | 等级 | 处置建议 | 状态 |
|---|---|---|---|---|---|---|
| `RISK-REQ-01` | 读者类型**确定方式**未裁决（注册自选 / 管理员指定 / 凭据审核） | `TBD-002` | 影响 `FR-001` 注册流程、`05` §3.2 D-1「类型可变更」的实现路径、借期与上限取数时机 | 中 | 采纳 `02` 建议默认（读者自选 + 管理员可修正）；若要求凭据审核，须新增审核态并回改 `FR-001` / `05` / `13` | 开放 |
| `RISK-REQ-02` | 50 元封顶的**计数口径**未裁决（当前未缴累计额 / 历史累计额） | `TBD-003`、`BR-010`、`BR-013` | 黑名单判定与借阅限制结果不同；缴清后是否立即解除存在两种实现 | 中 | 采纳默认（当前未缴累计额，缴清清零）；须在实现期保证「未缴累计额」只有一个计算入口（`FineAccrualService`） | 开放 |
| `RISK-REQ-03` | 每日计提的**执行时点与补提口径**未裁决 | `TBD-004`、`FR-017`、NFR-009 | 跨天未运行时是否补提影响罚款金额；幂等口径不明确易产生重复计提或漏提 | 中 | 采纳默认（固定时刻每日一次，按逾期自然日计算总额，天然可补提，同自然日不重复计提）；须在 `15` 中补「跨天补提」自动化用例 | 开放 |
| `RISK-REQ-04` | 预约保留到期的**扫描频率**未裁决 | `TBD-005`、`FR-015`、`BR-017` | 若要求小时级，须改调度设计（当前与计提共用每日任务） | 低 | 采纳默认（共用每日任务）；若业务要求更快，须走第五条变更流程改 `07` §12 与 `03` `UC-22` | 开放 |
| `RISK-REQ-05` | **系统管理员能否预约**未裁决 | `TBD-006`、`BR-028`、`H-4` | 权限矩阵硬约束的实现分支；与「不可借书」是否完全对齐 | 中 | 采纳默认（不可预约）；须保证 `canReserve()` 与 `canBorrow()` 同样「恒 false」，不散落判断 | 开放 |
| `RISK-REQ-06` | **未缴赔偿款是否禁止借阅**未裁决 | `TBD-007`、`BR-011`、`BR-012`、`H-3` | `BR-004` 只列 4 项前置校验；若采纳则须新增第 5 项校验，影响 `UC-09` 与借书失败原因码 | 中 | 采纳默认（未缴即禁借）；须同步 `02` `BR-004` 与 `03` `UC-09` 的前置校验清单，避免两处口径不一致 | 开放 |
| `RISK-REQ-07` | **初始系统管理员账号来源**未实现（种子数据尚未产出） | `TBD-008`、`FR-003`、NFR-005 | 全新环境无账号可登录，**阻塞**登录、权限与验收演示 | **高** | 必须在 `16-tasks.md` 与 `infrastructure.seed` 中落地；种子口令须满足 8 位含数字与字母并以 BCrypt 存储 | 开放（待实现） |
| `RISK-REQ-08` | 预约保留副本的**取书执行者**未裁决 | `TBD-009`、`BR-021`、`H-5` | 影响借书入口的权限校验与 `UC-09` 步骤 5 | 中 | 采纳默认（图书管理员柜台办理，与借书同一入口） | 开放 |
| `RISK-REQ-09` | **借阅证号生成规则**未定 | `TBD-010`、`BR-027` | 并发注册时号段冲突；长度与唯一索引（`VARCHAR(20)`）不匹配 | 低 | 采纳默认（`L` + 年份 + 6 位序列）；须在实现期以 DB 唯一索引兜底并重试 | 已缓解 |
| `RISK-REQ-10` | 借阅证**注销主体**未裁决 | `TBD-011`、`FR-002`、`BR-026` | 若允许读者自助注销，须新增自助入口与权限项 | 低 | 采纳默认（仅系统管理员，见 `07` §7.4） | 开放 |
| `RISK-REQ-11` | 标题 / 副本**删除方式**按默认取逻辑删除，尚未经人类确认 | `TBD-012`、`NFR-008` | 若审查要求物理删除，则 `13` 的 `deleted` 列、`RESTRICT` 外键与历史追溯口径全部返工 | 中 | 采纳默认（一律逻辑删除）；确认后须同步 `13` §2 与 `05` §3.4 | 开放 |
| `RISK-REQ-12` | 「规则数值须配置化」的**边界**存在歧义：借期 / 上限 / 费率在配置表，而**50 元封顶、3 天保留、3 本预约上限、30 分钟会话**不在配置表 | 第七条第 3 款、`BR-010` / `BR-013` / `BR-017` / `BR-018`、`FR-004`、`02` 附录 A | 可能被判定为违反第七条第 3 款；或反过来被要求把四类数值也做成配置表，扩大范围 | 低（**原高**，2026-09-24 裁决后关闭） | **已裁决（选项 A）**：第七条第 3 款的适用对象为**借阅与罚款规则**（`borrow_policies` / `fine_rules`，`BR-014` 专指）；四类数值为**需求常量**，须以配置项（`application.yml`）或领域层单一入口（`FineAccrualService` / `ReservationService`）承载，**禁止**散落硬编码与按类型分支；**不**新增配置表。已回写 `07` §10.2 与 `13` §2（见 §12 裁决 2） | **已关闭** |
| `RISK-REQ-13` | 检索口径落差：`FR-023` 仅支持**精确或前缀**匹配 | `FR-023` 验收 7、`00` §8 | 需求方 / 用户可能期望模糊匹配或分词检索，验收时产生争议 | 中 | 在 `14-api-spec.md` 与前端检索框明确「前缀匹配」提示；若要求模糊检索须走第五条变更流程 | 开放 |
| `RISK-REQ-14` | 需求**双编号体系**：需求方清单 16 项与本文件 `FR-001`~`FR-023` 不同源 | `02` 附录 C | 需求方按清单逐项验收时追溯成本上升 | 低 | 已有附录 C 映射（16/16 覆盖），交付时随 `02` 一并提交 | 已缓解 |
| `RISK-REQ-15` | 「**不做**」清单被下游误实现 | `02` §2.3、第十条第 1 款 | 出现「损坏」状态、续借、挂失 / 冻结、催还通知、报表统计、支付流水、条码扫描即判定越界 | **高** | `18-review-checklist.md` 已列为各工件的硬检查项；测试命名亦须禁用这些词（第九条第 7 款） | 已缓解（须持续核查） |
| `RISK-REQ-16` | 性能指标缺少**可测阈值**：NFR-001 仅描述 `D3` 规模，未给出响应时间 / 并发的具体数值 | NFR-001、`02` 附录 A | 性能验收不可判定；`15-test-plan.md` 无法编写断言 | 中 | 在 `15` 中补阈值（建议：单接口 P95 < 1s、分页查询 < 2s），或由人类确认「以主观可用为准」 | 开放 |

---

## 4. 建模风险（`RISK-MOD-x`）

| 编号 | 风险 | 来源 / 触发点 | 潜在影响 | 等级 | 处置建议 | 状态 |
|---|---|---|---|---|---|---|
| `RISK-MOD-01` | 读者类型**跨子类修正**需「重建实体」（学生 → 教师） | `05` §3.2 D-1、`05` 附录 A 遗留风险 1、`13` §7.2 `DB-D-1` | 实现期若处理不当，`id`、`BorrowCard`、历史借阅与罚款记录可能断裂 | 中（**原高**，2026-09-24 裁决后降为残余风险） | 已裁决**保留四个学生子类**，按 `13` `DB-D-1`：DB 层退化为一次 `UPDATE`（改 `reader_type` 与身份列，`id` 与借阅证不变）；由 `ReaderFactory` 按新类型构造对象后**以同一 `id` 回写**，**禁止** `INSERT` 新行；实现期须补集成测试验证「`id` / 借阅证号 / 历史借阅与罚款记录连续、新借书按新读者类型策略」（见 §12 裁决 1） | **已缓解（待验证）** |
| `RISK-MOD-02` | 学生读者建模为**四个具体子类**是否过重（是否按 `H-2` 退化） | `05` §10 `H-2`、`04` 与 `06` 继承树 | 类与表结构复杂度上升；若退化须重绘 `06` 并回改 `05` / `13` | 低 | **已裁决：保留四个学生子类**，`05` §10 `H-2` 的退化方案**不采纳**（2026-09-24）；`04` / `06` 继承树与 `13` `DB-D-1` 单表映射维持不变（见 §12 裁决 1） | **已关闭** |
| `RISK-MOD-03` | `ItemType` 由「子类 + `language`」推导（`H-3`），而 `13` 将其落为**派生冗余列** | `05` `H-3`、`13` `DB-ASM-03` | 两处口径若不一致（子类表与 `item_type` 列不符），罚款费率取错 | 中 | 由 `LibraryItemFactory` 单点写入并加集成测试；若严格按 `H-3` 不落列，须改为 JOIN 后在 Java 侧推导 | 开放 |
| `RISK-MOD-04` | `06` 由 Agent 补齐的 **5 个枚举**（`Language` / `AdminStatus` / `Action` / `ActionType` / `Result`）未在 `05` §5 枚举表中列出 | `06`、`19` 第 9 次、`05` §5 | 图与文档不一致；`ActionType` 取值直接决定 `13` 的 `ck_audit_action` 取值域 | 中 | 二选一：补登记进 `05` §5，或从 `06` 移除；`13` §5.15 已按 `05` §4.16 的 14 个取值写 CHECK，须与裁决结果一致 | 开放 |
| `RISK-MOD-05` | `04` 用例图中 `Student` / `Teacher` **泛化自 `Reader`**，与 `03` §2.2「读者类型是属性而非独立参与者」口径冲突 | `04`、`03` §2.2、`BR-001`、`19` 第 7 次 | 图即规格（第三条），实现期取哪一侧不确定 | 中 | 裁决以哪侧为准并同步另一侧 | 开放 |
| `RISK-MOD-06` | `04` 引入 `«extend»`（`UC-24 → UC-03`），而 `03` §4.2 明确排除三组 `«extend»` | `04`、`03` §4.2 | 用例关系图与文本不一致 | 低 | 确认该条 `«extend»` 是否保留；保留则同步 `03` §4.2 | 开放 |
| `RISK-MOD-07` | **UML 渲染未充分验证**：`04` 未做实机渲染；`08` 仅做语法级校验（本机无 Graphviz，排版未渲染） | `19` 第 7 / 11 次、第三条第 5 款 | 交付前可能存在不可渲染的 `.puml`，违反附则 A #1 | 中 | 安装 Graphviz 后对 `04` / `06` / `08` 完整渲染一次并留痕；渲染产物不得入库（禁图片形式 UML） | 待验证 |
| `RISK-MOD-08` | 状态迁移**仅以文字表**表达（`E2` / `R-19`，无状态图），实现期可能遗漏时间驱动迁移 | `05` §8、`BR-021`、`FR-015` | 遗漏「预约保留 3 天到期顺延下一位」「保留期届满无下一位转在馆」等 4 条必要迁移 | 中 | `15-test-plan.md` 须为 11 行副本迁移与 4 条时间驱动迁移逐条补测试；`16` 中列为实现任务 | 待验证 |
| `RISK-MOD-09` | 派生状态（黑名单、未缴累计额、在借副本数、队首）**不落库** | `05` §4.1 `H-1`、`13` §3.2 | 若实现期在多处各自计算，口径分裂（如封顶只算超期） | 中 | 统一由 `FineAccrualService` / `ReservationRepository` 单一入口计算；禁止新增冗余列 | 已缓解（须持续核查） |
| `RISK-MOD-10` | 预约**队列无实体**，`UC-20` 重排与 `UC-22` 顺延可能并发 | `05` §4.11、附录 A 遗留风险 2、`13` §5.11 | 位次错乱、重复保留、队首判定错误 | 中 | 同标题预约行级锁 / 乐观锁 + `version`；`13` 已不建 `queue_position` 唯一约束以允许重排中间态 | 待验证 |
| `RISK-MOD-11` | 用例**粒度**合并（`UC-13` / `UC-14` / `UC-17` 把添加 / 删除 / 修改合为一条） | `03` §3、`19` 第 6 次 | 与需求方清单的逐项追溯要求可能不一致 | 低 | 以 `02` 附录 C 建立映射；若需求方要求逐项用例，须拆分并重绘 `04` | 开放 |
| `RISK-MOD-12` | `06` 中**领域服务方法签名**为 `05` §6 的概括表达，尚未定稿 | `06`、`19` 第 10 次 | 与 `09-design-model.md`、`10`~`12` 时序图不一致 | 低 | `09` / `10`~`12` 成文后回填 `06`（第十一条第 4 款同轮一致） | 开放 |
| `RISK-MOD-13` | `06` 与 `05` v1.1 §4 的**属性 / 方法逐类一致性**未复核 | `06`、`05` §4、`19` 遗留 #13 | 类名、属性名与代码不一致 | 中 | 在 `09-design-model.md` 成文时逐类核对并留痕 | 待验证 |
| `RISK-MOD-14` | `BorrowCard` 与 `Reader` 同聚合（`H-4`），但 `13` 独立建表 | `05` §10 `H-4`、`13` §5.2 | 可能被误读为「可独立创建借阅证」，出现有证无人的中间态 | 低 | `13` §5.2 已注明「写入仍随 `Reader` 聚合在同一事务内」；实现期不得暴露独立发证接口 | 已缓解 |

---

## 5. 架构风险（`RISK-ARC-x`）

| 编号 | 风险 | 来源 / 触发点 | 潜在影响 | 等级 | 处置建议 | 状态 |
|---|---|---|---|---|---|---|
| `RISK-ARC-01` | 分层依赖**仅靠代码评审**保证，未引入编译期强制 | `07` §3.2 第 6 条、`ASM-05` | Controller 直连 Repository、领域层反向依赖等违约难以及时发现 | 中 | 若同意引入 `ArchUnit` 须先取得人类同意（第十二条第 4 款）；未获同意则以 `18` 清单 + 评审兜底 | 开放 |
| `RISK-ARC-02` | 根包名 `com.example.library` 为**示例值** | `07` `ASM-02` | 若为课程指定包名，须同轮修订 `07` §5 与 `08`、`src/` | 中 | 确认后同轮修订 | 开放 |
| `RISK-ARC-03` | 顶层包**固定 5 个**（含 `common`） | `07` §5.1、`ASM-01` | 新增顶层包将破坏与 `08` 的一致性 | 低 | 变更须走第五条流程并同步 `08` | 已缓解 |
| `RISK-ARC-04` | DTO 归属**应用层**而非独立契约包 | `07` `ASM-03` | 若审查要求契约包，包结构与依赖方向需调整 | 低 | 确认后同轮修订 `07` / `08` / `09` | 开放 |
| `RISK-ARC-05` | `Action` 权限枚举须与 `06` 一致 | `07` `ASM-06`、`06` | 权限矩阵落地与类图不一致 | 中 | `09` 成文时与 `06` 对齐；合并 `RISK-MOD-04` 一并裁决 | 开放 |
| `RISK-ARC-06` | 会话由应用层 `SessionStore` **内存**管理，重启即失效，且不集群 | `07` §7.2、`13` `DB-ASM-07` | 单机演示无影响；若要求持久化会话需新增存储 | 低 | 确认单机形态（`T1`、第十二条第 3 款） | 已缓解 |
| `RISK-ARC-07` | 定时任务 Runner **须可被测试单独触发**（注入 `Clock` / 直接调用），否则不可测 | `07` §4.5、`DP-10`、NFR-005 第 5 款 | 罚款计提与到期顺延无法自动化验证，违反门禁 | 中 | `15-test-plan.md` 须给出触发方式；实现期禁止在领域层直接读 `LocalDate.now()` | 待验证 |
| `RISK-ARC-08` | H2 **双模式混用**风险（运行文件模式 vs 测试内存模式） | `R-8`、NFR-002 第 3 款 | 测试数据污染演示数据，或反之 | 中 | 配置分离（`application.yml` / `application-test.yml`）；`15` 中加「数据源断言」测试 | 待验证 |
| `RISK-ARC-09` | 文件模式产物 `./data/` 未加入 `.gitignore` | NFR-002 第 4 款、第十二条第 5 款 | 数据库文件入库 | 低 | 交付前检查 `.gitignore` | 待验证 |
| `RISK-ARC-10` | 审计写入 `REQUIRES_NEW` 的交互：业务回滚后审计留存；审计写入**失败不得阻断**主流程 | `07` §9.4、`BR-030` | 实现若把审计并入业务事务，回滚后审计丢失；或审计异常导致业务失败 | 中 | `infrastructure.audit.AuditRecorderImpl` 单一实现；`15` 补「业务失败后审计仍在」的集成用例 | 待验证 |
| `RISK-ARC-11` | 并发借出依赖**条件更新 + `@Version`**；若实现退化为「先查后写」 | `07` §9.5、`NFR-009` 第 3 条 | 同一副本被重复借出，核心链路不可靠 | **高** | 必须 `UPDATE ... WHERE id=? AND status=?` 判影响行数；`15` 补并发借出测试（409） | 待验证 |
| `RISK-ARC-12` | 规则**调用点唯一**被绕过（自行读 `max_copies` / `daily_rate` 计算） | `BR-014`、`07` §11.2、`DP-05` / `DP-06` | 规则改动后部分路径不生效，出现第二处真相 | **高** | 仅 `BorrowPolicyProvider` / `FineRuleProvider` 可取规则；`18` 清单逐项核查；禁止 `switch` 分支 | 待验证 |
| `RISK-ARC-13` | 前端 `web/`（Vue 3 + Vite）与后端 dev 代理、构建产物静态托管**未验证** | `07` `ASM-08`、`08` | 部署形态不可用 | 低 | `16-tasks.md` 中加入「本地一键启动」验证任务 | 待验证 |
| `RISK-ARC-14` | 异常 → HTTP 状态码映射与 **`14-api-spec.md` 契约**的一致性（`14` 仍为 0 字节） | `07` §8.3、`06` | 实现与契约不一致即违约（第六条第 5 款） | 中 | `14` 成文后同轮核对状态码表；业务规则拒绝须携带具体原因码 | 开放 |
| `RISK-ARC-15` | 9 个**必须原子化**的用例若在实现中拆成多事务 | `07` §9.2、`TX-02` | 出现「有账号无借阅证」、借书成功但副本未置位等部分提交 | 中 | `15` 为每个原子用例补「异常后回滚」测试 | 待验证 |

---

## 6. 数据库风险（`RISK-DB-x`）

| 编号 | 风险 | 来源 / 触发点 | 潜在影响 | 等级 | 处置建议 | 状态 |
|---|---|---|---|---|---|---|
| `RISK-DB-01` | 规则配置表命名（`borrow_policies` / `fine_rules`）与 `constitution` 第七条第 3 款写作的 `borrowing_rule` / `fine_rule` **不一致** | `13` §0 冲突 1、`DB-ASM-01` | 可能被判定违反宪法字面表述 | **高** | 人类裁决：以 `constitution` 为准则回写 `13` 全部表名与索引名；以 `13` 为准则按第五条修订 `constitution` | 开放 |
| `RISK-DB-02` | `AdminAccount` 拆为 `librarians` / `system_admins` 后，**跨表 `username` 唯一**无 DB 约束 | `13` `DB-ASM-02`、`DB-D-3` | 两个角色出现同名账号，认证歧义 | 中 | 应用层 `AdminAccountRepository.findByUsername` 双表判重（唯一入口）；或改为 `JOINED` + 父表 `admin_accounts` | 开放 |
| `RISK-DB-03` | `library_items.item_type` 为**派生冗余列**，与子类表的一致性仅靠应用层 | `13` `DB-ASM-03`、`05` `H-3` | 子类与 `item_type` 不符 → 罚款费率取错 | 中 | `LibraryItemFactory` 单点写入 + 集成测试；禁止触发器（规则不落 SQL） | 待验证 |
| `RISK-DB-04` | `library_items.isbn` 设为 NOT NULL，**论文无 ISBN** 的登记口径未定 | `13` `DB-ASM-04` | 论文录入受阻或填入无意义值 | 中 | 确认论文以校内学位论文档案号登记，或改可空并调整 `uk_items_isbn_type` | 开放 |
| `RISK-DB-05` | H2 2.x **计算列**（`loans.open_copy_key`、`reservations.active_slot`）及其上唯一索引**未实测** | `13` `DB-ASM-05`、`NFR-009` | 两个关键并发约束失效（同一副本重复借出、重复预约） | **高** | 集成测试实测 `GENERATED ALWAYS AS` 与唯一索引；不兼容则退化为「应用层维护可空列 + 唯一索引」或行级锁 | 待验证 |
| `RISK-DB-06` | `readers.student_no` 必填性未定（`05` §4.2 未明确「可选」归属） | `13` `DB-ASM-06` | 学生注册校验口径不一致 | 低 | 确认「学生必填学号、教师无学号」（现有 `ck_readers_identity`） | 开放 |
| `RISK-DB-07` | 审查提出 `library_item.barcode`，本设计**不建该列** | `13` `DB-ASM-09`、`05` §5、`02` §2.3 | 若需求方期望条码字段，则属需求缺口；若擅自新增则越界（条码扫描在不做清单） | 中 | 裁决：不建（唯一性由 `item_copies.copy_number` 承担），或走第五条流程在 `05` / `02` 新增术语后再回写 `13` | 开放 |
| `RISK-DB-08` | 命名口径分歧（单数表名 / 缩写列名 vs 复数表名 / snake_case 全称） | `13` `DB-ASM-10` | 与代码、API 字段命名不一致 | 低 | 确认一套并全文件重命名 + 同步 `13` §7.5 PO 对照 | 开放 |
| `RISK-DB-09` | 中文枚举值**直接落库**（`在馆` / `中文图书` / `专科生` 等） | 第二条第 4 款、`13` §7.4 | 术语一旦变更需 DDL + 数据迁移；排序与比较依赖字符序 | 中 | 术语冻结（第二条），变更须走第五条流程；实现期以枚举常量映射，禁止字面量散落 | 已缓解 |
| `RISK-DB-10` | 全部外键 `RESTRICT` + 一律逻辑删除 ⇒ **数据只增不减** | `13` §4.2、`NFR-008` | 长期运行后历史数据累积，无清理路径 | 低 | 规模 `D3`（10 万流水）可接受；若需归档须走变更流程 | 已缓解 |
| `RISK-DB-11` | `audit_logs` 无外键、只追加，`actor_id` / `target_id` 为**弱引用字符串** | `13` §5.15、`FR-005` 验收 4 | 追溯时无法直接 JOIN；类型混排（读者 / 管理员 id 同列） | 低 | 以 `actor_role` + `target_type` 区分；查询走 `idx_audit_target` | 已缓解 |
| `RISK-DB-12` | `loans.due_date` 为**快照**，规则变更不追溯；实现若重算则违约 | `05` §4.12 D-2、`13` §5.10 | 已借记录被追溯改期，违反 `FR-020` 验收 3 | 中 | 禁止任何更新 `due_date` 的写路径（亦符合 `BR-006` 不可续借） | 待验证 |
| `RISK-DB-13` | 未设通用 `created_at` / `updated_at`（有意取舍），排障信息较少 | `13` §3.2、`05` §3.4 | 问题定位困难；若审查要求审计级时间戳须增列 | 低 | 以 `version` + 审计日志补足；如需增列须同步 `05` §3.4 的取舍说明 | 开放 |
| `RISK-DB-14` | 索引 39 条，存在**写入放大** | `13` §6.1、日均 300 借还 | 批量计提与导入时写入变慢 | 低 | 单表 ≤ 4 个二级索引已约束；`D3` 规模可接受 | 已缓解 |
| `RISK-DB-15` | 规则种子 **5 + 5 行缺失**即导致计提失败并留痕 | `13` §9、`UC-21 E1` | 缺任一行费率即无法计提，且**禁止**按 0 或默认值静默处理 | 中 | 种子数据初始化须幂等且可校验行数；`15` 补「缺费率即失败」用例 | 待验证 |

---

## 7. 权限风险（`RISK-SEC-x`）

| 编号 | 风险 | 来源 / 触发点 | 潜在影响 | 等级 | 处置建议 | 状态 |
|---|---|---|---|---|---|---|
| `RISK-SEC-01` | `AccessGuard.require()` 若在某应用服务**漏调用**即产生越权 | `07` §7.3、`SEC-03`、第八条第 3 款 | 权限矩阵失效 | **高** | 每个公开用例方法**首行**显式调用；`18` 清单逐项核查；`15` 补「权限失败」场景（门禁五场景之一） | 待验证 |
| `RISK-SEC-02` | 数据级权限：读者侧接口的 `readerId` 必须**取自会话** | `07` §7.5、`FR-011` 验收 3、`FR-018` 验收 5 | 传入他人标识即可读取 / 操作他人数据 | **高** | 禁止以客户端传入标识作为授权依据；越权 → 403 且不返回数据 | 待验证 |
| `RISK-SEC-03` | 「系统管理员不可借书 / 不可预约」若以 `role` 判断**散落实现** | `BR-028`、`07` §7.6 | 出现管理员借书的越权路径 | 中 | 仅由 `AdminAccount.canBorrow()` / `canReserve()` 恒 false 表达，在 `BorrowingService` / `ReservationService` 入口前置拦截 | 待验证 |
| `RISK-SEC-04` | 「图书管理员不能修改**自己**的账号信息」 | `BR-028`、`FR-022` 验收 3 | 自查自改权限提升 | 低 | `AdminAccountApplicationService.update()` 校验「操作者 == 被改账号」则拒绝 | 待验证 |
| `RISK-SEC-05` | 「不可删除**最后一个**系统管理员」在分表后需跨表计数 | `FR-022` 验收 4、`13` §5.4 | 停用最后一个系统管理员后系统无管理员 | 中 | 领域层校验 `COUNT(system_admins WHERE status='正常') > 1`；禁止用触发器 | 待验证 |
| `RISK-SEC-06` | 越权被降级为「返回空列表」而非 **403 + 审计** | `07` §7.6 / `SEC-08`、第八条第 3 款 | 越权行为无审计留痕，违反 NFR-004 第 4 款 | 中 | `18` 清单核查；`15` 补断言「403 + 审计表新增一条 result=失败」 | 待验证 |
| `RISK-SEC-07` | 审计**覆盖范围**遗漏（`FR-005` 13 类操作 + 登录失败；预约顺延 / 取消必落） | `05` §4.16、`FR-015` 验收 7、`FR-003` 验收 5 | 合规要求不达标（`P2`、`BR-030`） | 中 | 以 `AuditRecorder` 统一入口；`15` 逐类核查 | 待验证 |
| `RISK-SEC-08` | 登录失败须**统一提示**且落审计（防账号枚举） | `FR-003` 验收 5、`07` §7.1、`UC-23` | 提示区分「用户不存在 / 口令错误」→ 账号枚举 | 中 | 领域层 `authenticate()` 返回布尔，提示在应用层统一；失败必写审计 | 待验证 |
| `RISK-SEC-09` | 会话 30 分钟为**配置项**，禁止硬编码参与业务判断 | `FR-004` 验收 3、`07` §7.2 | 改配置不生效 | 低 | 置于 `application.yml`；`15` 补会话超时测试 | 待验证 |
| `RISK-SEC-10` | 口令强度（8 位含数字与字母）与 BCrypt 60 字符存储 | `BR-029`、第八条第 4 款、`13` §5.1 / §5.3 | 弱口令或明文存储 | 低 | 领域层 `register()` / `create()` 校验；DB 列 `CHAR(60)` | 待验证 |
| `RISK-SEC-11` | 前端以**隐藏按钮 / 隐藏菜单**充当权限控制 | 第八条第 3 款、`07` §7.8 | 后端未校验即越权 | 中 | 后端为唯一判定点；`18` 清单核查前端不得有权限判断 | 待验证 |
| `RISK-SEC-12` | 审计**独立事务**（`REQUIRES_NEW`）实现与「写入失败不阻断业务」 | `07` §9.4、`BR-030` | 审计丢失或拖垮主流程（同 `RISK-ARC-10`） | 中 | 合并处置 | 待验证 |
| `RISK-SEC-13` | 「不落审计」的边界被扩大（注册 / 自动发证、只读查询、定时计提本就不审计） | `05` §4.16 第 2 条、`H-1` | 应审计的写操作被误归入例外 | 低 | `18` 清单按 `FR-005` 验收 1 逐条核对 | 待验证 |

---

## 8. Agent 使用风险（`RISK-AGT-x`）

| 编号 | 风险 | 来源 / 触发点 | 潜在影响 | 等级 | 处置建议 | 状态 |
|---|---|---|---|---|---|---|
| `RISK-AGT-01` | **留痕缺口**：`07` / `08` / `13` 均曾因「仅修改本文件」未登记（`ASM-07` / `DB-ASM-08`）；本次生成 `17` / `18` 亦因同一限制**尚未登记** | 第四条第 5 款、`19` 第 10~14 次 | 交付前门禁 #3 不通过 | **高** | 每次生成后立即登记；本次须由人类补齐或另行授权后补记第 15 次 | 开放 |
| `RISK-AGT-02` | **默认处理被误当作人类确认**：`TBD-002`~`012`、`03` `H-1`~`H-5`、`05` `H-1`~`H-4`、`07` `ASM-01`~`08`、`13` `DB-ASM-01`~`10` 均属 AI 默认 | 第三条第 3 款、第四条第 3 款 | 未经裁决的口径进入实现，事后返工 | **高** | 本文件逐条标注「开放（须人类裁决）」；裁决结论回填 `01`「复审」小节与 `19`「人工审查结果」 | 开放 |
| `RISK-AGT-03` | **冻结基线被擅改**：`00` / `01` 为冻结基线，下游「顺手修正」 | 第五条第 2、4 款 | 基线漂移，需求追溯失效 | 中 | 发现上游缺陷一律走第五条四步流程 | 已缓解（须持续核查） |
| `RISK-AGT-04` | **自行补全基线未定义**的行为（如 `barcode`、`06` 补充的 5 个枚举） | 第二条第 3 款、`RISK-MOD-04` / `RISK-DB-07` | 越界实现或术语新增 | 中 | 一律先登记为待确认项，禁止静默假设 | 开放 |
| `RISK-AGT-05` | **术语漂移**：中文枚举被简写 / 英译 / 改写（如 `在馆` → `ON_SHELF`） | 第二条第 4 款、第九条第 7 款 | 与基线逐字不一致，测试命名亦被污染 | 中 | `18` 清单逐项核查术语；代码与 DB 均用中文枚举值 | 已缓解（须持续核查） |
| `RISK-AGT-06` | **文档链不同步**：`09`~`12`、`14`~`18` 中多数仍为 0 字节；`04`~`12` 与 `02` / `03` / `05` / `09` / `13` / `14` / `15` / `16` 须同轮一致 | 第十一条第 4 款 | 交付物缺失、口径分裂 | **高** | 按 `16-tasks.md` 排期补齐；每次成文同轮更新相关文档 | 开放 |
| `RISK-AGT-07` | **未审查产出被当作下游输入**：`02`~`08`、`13` 均为「草稿，待人工审查」 | 第四条第 1 款 | 缺陷被放大到实现 | **高** | 用 `18-review-checklist.md` 逐项打勾并在 `19` 落盘审查结论后方可进入实现 | 开放 |
| `RISK-AGT-08` | Agent 自行 `commit` / `push` | 第五条第 6 款、第十一条第 2 款 | 未授权提交 | 低 | 已由人类明确指令推送一次（`main` → `origin/main`）；后续仍须逐次授权 | 已缓解 |
| `RISK-AGT-09` | `19` 记录字段不完整（工具 / 阶段 / 人工审查结果） | 第四条第 5 款、`19` 第 1~5 次 | 留痕不可追溯 | 中 | 工具已统一为 CodeBuddy；「人工审查结果」仍大量为「待本人填写」 | 开放 |
| `RISK-AGT-10` | **测试门禁未落实**：五核心场景（借书 / 还书 / 预约 / 超期罚款 / 权限失败）须自动化，`15-test-plan.md` 仍为 0 字节 | 第九条第 1、2 款、`07` §4.5 | 交付判定无依据 | **高** | `15` 成文 + 实现阶段优先打通纵向切片（`E1`、第九条第 6 款） | 开放 |
| `RISK-AGT-11` | 第三方依赖未经同意即引入（`ArchUnit` / `MapStruct` / 渲染 jar） | 第十二条第 4 款、`ASM-04` / `ASM-05` | 课程范围外依赖 | 中 | 当前**均未引入**；渲染 jar 仅存临时目录、未入库（已有处理） | 已缓解 |
| `RISK-AGT-12` | **范围膨胀**：实现超出 `00` §4 业务清单与 `01` `E1` 裁剪结论 | 第一条第 3 款 | 交付范围失控 | 中 | 新增能力一律先走第五条变更流程 | 已缓解（须持续核查） |

---

## 9. 高风险项汇总（交付前必须关闭）

| # | 编号 | 风险一句话 | 关闭条件 |
|---|---|---|---|
| 1 | `RISK-REQ-07` | 初始系统管理员账号（种子）未实现 | 种子数据落地，全新环境可登录 |
| 2 | `RISK-REQ-12` | 规则「配置化」边界歧义（封顶 50 / 保留 3 天 / 预约上限 3 / 会话 30 分钟） | **已裁决**（选项 A：仅借阅与罚款规则须入配置表，其余为需求常量 + 配置项/单一入口）；回写 `07` §10.2 与 `13` §2 **已完成** |
| 3 | `RISK-REQ-15` | 「不做」清单被下游误实现 | `18` 各工件核查通过 |
| 4 | `RISK-MOD-01` | 读者类型跨子类修正「重建实体」易断裂 | **已裁决**（保留子类，按 `DB-D-1` 一次 `UPDATE` 实现）；剩余关闭条件：集成测试验证 `id` / 借阅证 / 历史记录连续 |
| 5 | `RISK-ARC-11` | 并发借出退化为「先查后写」 | 条件更新 + `@Version` 落地并有并发测试 |
| 6 | `RISK-ARC-12` | 规则调用点唯一被绕过 | 仅 Provider 可取规则，无 `switch` 分支 |
| 7 | `RISK-DB-01` | 规则表名与 `constitution` 字面表述不一致 | 人类裁决并同轮回写 |
| 8 | `RISK-DB-05` | H2 计算列可索引性未实测 | 集成测试通过，或退化方案落地 |
| 9 | `RISK-SEC-01` | `AccessGuard` 漏调用 | 逐服务核查 + 权限失败测试 |
| 10 | `RISK-SEC-02` | 数据级权限未取会话标识 | 逐接口核查 + 越权测试 |
| 11 | `RISK-AGT-01` | 留痕缺口（含本次 `17` / `18`） | `19` 登记完毕 |
| 12 | `RISK-AGT-02` | 默认处理被误当确认 | 关键 `TBD` / `H` / `ASM` / `DB-ASM` 逐条裁决 |
| 13 | `RISK-AGT-06` | 文档链不同步（`09`~`12`、`14`~`16` 空缺） | 成文排期落实 |
| 14 | `RISK-AGT-07` | 未审查产出被当下游输入 | `18` 打勾 + `19` 落盘审查结论 |
| 15 | `RISK-AGT-10` | 五核心场景自动化测试缺失 | `15` 成文且用例通过 |

---

## 10. 处置优先级建议

| 优先级 | 处置对象 | 时机 |
|---|---|---|
| P0 | `RISK-REQ-07`、`RISK-DB-05`、`RISK-ARC-11`、`RISK-SEC-01`、`RISK-SEC-02`、`RISK-AGT-10` | 进入编码前（影响可运行与验收） |
| P1 | `RISK-REQ-12`、`RISK-DB-01`、`RISK-MOD-01`、`RISK-AGT-02`、`RISK-AGT-07` | `09` / `16` 成文前（影响口径与合法性） |
| P2 | `RISK-MOD-03` / `04` / `05`、`RISK-ARC-01` / `07` / `10`、`RISK-DB-02` / `03` / `04`、`RISK-SEC-03`~`08` | 编码过程中逐项收口 |
| P3 | 其余中低风险 | 交付前随 `18-review-checklist.md` 一并核查 |

---

## 11. 与 `constitution.md` 的一致性自检

| # | 检查项 | 本文件落点 | 判定 |
|---|---|---|---|
| 1 | 假设与缺口**登记**而非留在对话中（第四条第 4 款、第十一条第 3 款） | §3~§8 全部条目 | 符合 |
| 2 | 显式区分「AI 默认处理」与「人类确认结论」（第三条第 3 款） | §2.1、各条目「状态」列 | 符合 |
| 3 | 未把推断表述为「用户已确认」（第四条第 3 款） | 全部默认项标注「开放」 | 符合 |
| 4 | 不擅自裁决上游 `TBD` / `H` / `ASM` / `DB-ASM` | §2.1、处置建议均为建议而非结论 | 符合 |
| 5 | 术语与枚举与基线逐字一致（第二条第 4 款） | 全文沿用 `02` 附录 A 原文 | 符合 |
| 6 | 未新增编号体系外的文档 / 图 | 仅 `17` / `18` 两个既有编号 | 符合 |
| 7 | 留痕冲突已声明（第一条第 2 款） | 文件头「留痕」字段、`RISK-AGT-01` | 符合（登记待补） |

---

## 12. 人类裁决记录

> 依第十三条第 2 款：人类裁决结果**必须**回写到相应文档；依第四条第 2 款：裁决须在 `01`「复审」小节与 `19-ai-usage-log.md` 落盘。本表登记本文件收到的裁决，并列出**待同步**的文档。

| # | 日期 | 裁决事项 | 裁决结论 | 受影响文档 | 本文件更新 | 待同步（须人类授权后执行） |
|---|---|---|---|---|---|---|
| 1 | 2026-09-24 | 学生读者是否保留 `StudentReader` 的**四个具体子类**（`05` §10 `H-2` 给出退化方案） | **保留四个学生子类**；`H-2` 的「`Reader` + 可变 `ReaderType`、取消子类」**不采纳** | `05` §3.2 / §4.2 / §10 `H-2`、`06` 继承树、`13` §7.2 `DB-D-1` / §5.1 | `RISK-MOD-02` 关闭（等级降为低）；`RISK-MOD-01` 降为中、状态「已缓解（待验证）」；§9 第 4 项标注已裁决 | ① `05` §10 `H-2` 补注「人类裁决：不采纳退化，保留子类」（须走第五条变更流程登记）；② `06` 类图继承树维持现状即可，但建议在图注中标注「已裁决保留」；③ `19-ai-usage-log.md` 登记本次裁决与留痕 |

| 2 | 2026-09-24 | 「规则数值须配置化」的**适用边界**（宪法第七条第 3 款） | **选项 A**：适用对象 = **借阅与罚款规则**（`borrow_policies` / `fine_rules` 两张配置表）；**50 元封顶、预约保留 3 天、单人预约上限 3、会话 30 分钟**为**需求常量**，不新增配置表 | `07` §10.2（`RULE-13` 不归属清单）、`13` §2（设计依据与约束）、`02` 附录 A | `RISK-REQ-12` 关闭（等级降为低）；§9 第 2 项标注已裁决；`07` §10.2 与 `13` §2 已回写 | ① `19-ai-usage-log.md` 登记本次裁决；② `15-test-plan.md` 补「四类常量不得散落硬编码」的静态检查或用例 |

**裁决 2 的实现约束（供 `16-tasks.md` 与 `src/` 直接引用）**

1. **入配置表**（系统管理员运行时可改，`FR-020`）：`borrow_policies`（`reader_type` × `max_copies` × `loan_period_days`）、`fine_rules`（`item_type` × `daily_rate` × `grace_period_days`）—— 只经 `BorrowPolicyProvider` / `FineRuleProvider` 读取。
2. **不入配置表**（需求常量）：50 元封顶、预约保留 3 天、单人预约上限 3 —— 判定逻辑集中在 `FineAccrualService` / `ReservationService` 单一入口；数值以 `application.yml` 配置项或**唯一**常量承载，**禁止**在 Controller / 应用服务 / SQL / 前端重复出现（第七条第 3 款、`07` §10.2 对本表的禁止要求不变）。
3. 会话 30 分钟：`07` §7.2 已定为 `application.yml` 配置项，禁止硬编码参与业务判断（`FR-004` 验收 3）。
4. `CHECK` / 触发器 / 视图中**不得**出现上述任何数值（含借期与费率）—— 规则不落 SQL。
5. 种子脚本是**唯一**允许出现规则字面量的位置（`07` §10.2）。

**裁决 1 的实现约束（供 `16-tasks.md` 与 `src/` 直接引用）**

1. 保留 `JuniorCollegeReader` / `UndergraduateReader` / `MasterReader` / `DoctoralReader` 四个子类与 `TeacherReader`。
2. 借期与可借上限**只**经 `readerType` 查 `BorrowPolicy`；**禁止** `instanceof` 按子类分支（`BR-014`）。
3. 跨子类修正（学生 → 教师）：`ReaderFactory` 按新类型构造对象，以**同一 `id`** 回写；DB 层为一次 `UPDATE readers SET reader_type='教师', student_no=NULL, employee_no=? WHERE id=?`；**禁止**新增读者行、禁止重新发证。
4. 已借出记录的 `due_date` 不因类型变更重算（`05` D-1 第 4 条）。
5. 集成测试须覆盖该路径（属 `15-test-plan.md` 待测点）。

---

## 附录 A 风险来源对照（上游编号 → 本文件）

| 上游编号 | 本文件条目 |
|---|---|
| `TBD-002` ~ `TBD-012` | `RISK-REQ-01` ~ `RISK-REQ-11` |
| `03` `H-1` ~ `H-5` | `RISK-SEC-13`、`RISK-REQ-11`、`RISK-REQ-06`、`RISK-REQ-05`、`RISK-REQ-08` |
| `05` `H-1` ~ `H-4` 与附录 A 遗留风险 | `RISK-MOD-09`、`RISK-MOD-02`、`RISK-MOD-03`、`RISK-MOD-14`、`RISK-MOD-01`、`RISK-MOD-10` |
| `07` `ASM-01` ~ `ASM-08` | `RISK-ARC-03`、`RISK-ARC-02`、`RISK-ARC-04`、`RISK-AGT-11`、`RISK-ARC-01`、`RISK-ARC-05`、`RISK-AGT-01`、`RISK-ARC-13` |
| `13` `DB-ASM-01` ~ `DB-ASM-10` | `RISK-DB-01`、`RISK-DB-02`、`RISK-DB-03`、`RISK-DB-04`、`RISK-DB-05`、`RISK-DB-06`、`RISK-ARC-06`、`RISK-AGT-01`、`RISK-DB-07`、`RISK-DB-08` |
| `19` 遗留事项 #1~#32 | 已按类别并入 §3~§8 |

---

## 附录 B 修订记录

| 版本 | 日期 | 修订内容 | 来源 |
|---|---|---|---|
| v1.0 | 2026-09-24 | 首版成文：① 登记约定与来源、等级口径；② 需求风险 16 项；③ 建模风险 14 项；④ 架构风险 15 项；⑤ 数据库风险 15 项；⑥ 权限风险 13 项；⑦ Agent 使用风险 12 项；⑧ 高风险汇总 15 项与处置优先级；⑨ 一致性自检；⑩ 上游编号对照 | `00`~`08`、`13`、`19`、`constitution.md`、人类指令 |
| v1.0.1 | 2026-09-24 | 登记人类裁决 1 项（**保留四个学生子类**，`H-2` 退化不采纳）：① `RISK-MOD-01` 由高降为中、状态改为「已缓解（待验证）」并写明实现约束；② `RISK-MOD-02` 关闭、等级降为低；③ §9 第 4 项标注已裁决；④ 新增 §12 人类裁决记录（含 5 条实现约束与待同步文档清单）；⑤ 文件头版本与「本次修订」、§1 高风险项计数同步 | 人类裁决（2026-09-24）、第十三条第 2 款、第四条第 2 款 |
| v1.0.2 | 2026-09-24 | 登记人类裁决 2 项（**配置化边界选项 A**）：① `RISK-REQ-12` 由高降为低并关闭，写明适用对象为借阅与罚款规则；② §9 第 2 项标注已裁决且回写已完成；③ §12 新增裁决 2 与「裁决 2 的实现约束」5 条；④ 已回写 `07-architecture.md` §10.2 与 `13-database-design.md` §2；⑤ 文件头版本与「本次修订」同步 | 人类裁决（2026-09-24）、第七条第 3 款、第十三条第 2 款 |

> **留痕要求（NFR-007 / 第四条第 5 款）**：本次由 Agent 生成，须在 `19-ai-usage-log.md` 新增一条记录（编号顺延为第 15 次），「人工审查结果」由人类补齐；本轮指令限定仅创建 `17` / `18`，登记条目尚未写入（`RISK-AGT-01`）。审查通过前，本文件不得作为 `16-tasks.md` 与 `src/` 的输入（第四条第 1 款）。

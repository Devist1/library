# 01 澄清问题清单（Clarifying Questions）

- **输入**：`specs/00-project-brief.md`
- **状态**：A1–E3 已回答完毕；AI 复审结果见文末「复审（Review）」小节（19 项，**已全部关闭，待确认 0 项**）
- **用途**：回答后作为 `02-requirements.md`、`03-use-cases.md`、`05-domain-model.md` 的输入
- **约定**：每个问题有唯一编号（如 `A1`）；复审项编号为 `R-*`，与问题编号区分。未确认项按「默认处理」推进，并作为假设记入 `17-risk-analysis.md`。

---

## 1. 参与者与角色

**A1. 读者类型的继承层次如何定义？**
Brief 第 3 节列出「读者 / 学生读者 / 教师读者」，第 5 节又列出「本科生 / 研究生 / 博士生 / 专科生 / 教师」。是否确认为：`读者` → `学生读者` → {`专科生`, `本科生`, `研究生`, `博士生`}，以及 `读者` → `教师读者`？第 3 节的「教师读者」与第 5 节的「教师」是同一概念吗？

**A2. 管理员角色的关系与多重角色？**
「图书管理员」与「系统管理员」是泛化关系（系统管理员是图书管理员的特化）还是两个平级角色？同一个自然人能否同时持有多个角色（例如某教师既是读者又协助办理借还）？本次是否需要「馆长 / 审批人」等额外角色？

---

## 2. 用例与业务流程

**U1. 「注册账号」与「办理借阅证」的关系？**
是一次操作完成，还是先注册账号、再由管理员审核发证的两步流程？借阅证是否有独立编号、有效期和状态（正常 / 挂失 / 冻结 / 注销）？是否包含补办与注销流程？

**U2. 借书与还书的操作主体是谁？**
由读者自助（Web / 自助机）完成，还是必须由图书管理员在柜台代为办理？两种方式是否需要同时支持？

**U3. Brief 第 4 节的 10 项业务是否就是完整用例清单？**
是否遗漏下列候选用例：读者挂失 / 注销借阅证、缴纳罚款、续借、催还通知、遗失赔偿、报表统计？「查询借阅信息」中读者只能查自己、管理员可查全部，对吗？

---

## 3. 业务规则

**B1. 规则数值由谁维护、存在哪里？**
读者类型 × 借出物类型的「借阅数量 / 期限 / 罚款费率」规则表，是数据库中的配置数据（管理员可改），还是代码中的硬编码常量？（这决定 `13-database-design.md` 是否需要 `borrowing_rule` 类表。）

**B2. 规则冲突时的优先级？**
当「读者类型」规定的期限与「借出物类型」规定的期限不一致时以谁为准（取小值 / 取交集 / 以借出物类型为准）？同一个读者同时受多套规则约束时如何裁决？

---

## 4. 借阅规则

**L1. 请给出 5 种读者类型的借阅参数具体数值。**
分别给出 `专科生 / 本科生 / 研究生 / 博士生 / 教师` 的「最大可借数量（册）」与「借阅期限（天）」。

**L2. 借出物类型是否影响借阅期限？是否允许重复借同一标题？**
中文图书 / 外文图书 / 中文杂志 / 外文杂志 / 论文 的期限是否相同？同一读者能否同时借同一「图书标题」下的多个副本？

**L3. 借阅的前置校验条件与续借规则？**
借阅前是否校验：有超期未还、有未缴罚款、已达数量上限、有未取预约？是否支持续借，续借次数上限、续借后期限、已被他人预约的书能否续借？

---

## 5. 罚款规则

**F1. 请给出 5 种借出物类型的超期规则与罚款费率。**
每种类型的「宽限期（天）」与「罚款单价（元/天）」。罚款是否区分读者类型（如教师减免）？

**F2. 罚款是否封顶？何时计提？**
是否有单册封顶 / 单次借阅封顶 / 单读者封顶金额？罚款是「归还时一次性结算」，还是每日定时任务累计计提？

**F3. 罚款的处理方式与对借阅的限制？**
由于 Brief 第 8 节明确暂不实现真实扣款，「计算超期罚款」的产出是仅生成应收记录（账目），还是需要管理员手工登记「已缴 / 减免」？罚款未结清是否禁止继续借阅，是否存在阈值或进入黑名单？

---

## 6. 预约规则

**R1. 预约的触发条件？**
只有某标题的**全部副本都已借出**时才允许预约，还是只要有在馆副本也可预约（到馆后再取）？

**R2. 预约队列的排序策略？**
严格按预约时间 FIFO，还是按读者类型优先（如教师优先）？是否允许「插队」或管理员手工调整队列？

**R3. 预约的保留与失效机制？**
副本归还且有预约时，是否进入「预约保留」状态并暂停对外借出？保留多少天？逾期未取如何处理（自动取消 / 顺延给下一位 / 记违约）？是否需要到馆通知（本项目是否要真实发邮件/短信）？每位读者的预约数量上限与主动取消规则如何？

---

## 7. 权限与安全

**P1. 请确认权限矩阵。**
「维护图书标题信息」「维护馆藏副本」「维护管理员」「计算/减免罚款」「查询全部借阅记录」分别属于哪个角色？系统管理员是否可以借书？图书管理员能否修改自己的账号信息？

**P2. 是否需要审计日志？**
是否需要记录「谁在什么时间做了什么操作」的审计日志（这对课程实验是可加分项，但会增加工作量）？

**P3. 认证方式与会话策略？**
采用本地用户名 + 口令吗？口令强度与哈希算法有无要求（如 BCrypt）？会话超时时间？既然 Brief 第 8 节排除了统一身份认证，是否要求预留第三方登录的扩展点？

---

## 8. 数据与领域模型

**D1. 副本的身份与状态集合？**
确认模型为「1 个图书标题 → N 个馆藏副本」，每个副本有唯一编号/条码？副本的状态集合是哪些：`在馆 / 已借出 / 预约保留 / 遗失 / 损坏 / 报废`？遗失与损坏是否在本项目范围内？

**D2. 「借出物类型」挂在标题还是副本上？**
中文/外文图书、中文/外文杂志、论文 —— 这是「图书标题」的属性，还是「馆藏副本」的属性？（例如同一书名同时存在中文版与外文版时如何区分。）

**D3. 数据留存与规模量级？**
读者注销、副本报废后，其历史借阅记录是否保留（逻辑删除 vs 物理删除）？预期的数据规模（读者数、标题数、副本数、日均借还次数）大约是多少？（用于 `07-architecture.md` 的选型论证。）

---

## 9. 架构与技术

**T1. 技术栈与系统形态？**
后端语言/框架（Java + Spring Boot / Python + FastAPI / Node.js + NestJS …）？数据库（MySQL / PostgreSQL / SQLite）？形态是前后端分离的 Web 应用，还是桌面应用/命令行？

**T2. 「分层架构 + MVC」的具体分层方案？**
期望的分层是「表现层 / 应用层 / 领域层 / 基础设施层」四层吗？是否要求引入 DDD 聚合与仓储（Repository）概念，还是经典三层即可？是否需要交付 REST API 契约（`14-api-spec.md` 用 OpenAPI 形式）？部署形态要求单机可运行，还是需要 Docker 化？

---

## 10. 测试与验收

**Q1. 自动化测试的范围与门槛？**
需要哪些层次：领域单元测试 / 集成测试（数据库）/ API 接口测试？是否有覆盖率门槛？是否需要可重复运行的种子数据（seed）与测试专用数据库（如 H2 / SQLite 内存库）？

**Q2. 验收标准是什么？**
以 `15-test-plan.md` 中全部自动化测试通过为准，还是还需要一份人工演示脚本（demo scenario）？关键用例（借书 / 还书 / 预约）是否必须端到端跑通？

---

## 11. 教学复杂度与交付范围

**E1. 范围优先级如何裁剪？**
是优先纵向打通「借书 → 还书」核心链路（含罚款、预约的关键分支），还是要求 Brief 第 4 节全部 10 项业务同等完备？

**E2. UML 交付清单是否就是已有的 6 张图？**
是否还需要补充状态图（如副本状态机、借阅状态机）或活动图？

**E3. AI 使用留痕与时间盒？**
`19-ai-usage-log.md` 需要记录到什么粒度（每次 Prompt / 每次生成物 / 每次人工修改）？项目的交付截止时间与可用人力是多少？（用于判断是否需要在 `17-risk-analysis.md` 中增加进度风险。）

---

## 人类回答

> 请在每个编号下作答。留空表示「暂不确定」。

### A1
确认为：`读者` → `学生读者` → {`专科生`, `本科生`, `研究生`, `博士生`}，以及 `读者` → `教师读者`第 3 节的「教师读者」与第 5 节的「教师」是同一概念
### A2
图书管理员作为借阅者的代理完成借阅图书，归还图书和查询借阅信息工作
系统管理员完成对系统的维护，对系统的维护主要包括办理借阅证，删除借阅证，添加管理员，删除管理员，添加图书，删除图书，添加标题信息，删除标题信息
「图书管理员」与「系统管理员」的分工不同,同一个自然人可以同时持有多个角色（例如某教师既是读者又协助办理借还）,不需要「馆长 / 审批人」等额外角色

> 语义澄清与补齐（依 `R-10` / `R-11` / `R-12` / `R-14` / `R-5` / `R-16` 确认）：
> 1. 「**办理借阅证**」等价于**注册账号时系统自动发证**（无审核环节），不单列为独立用例；「**删除借阅证**」等价于**读者注销**，即将状态置为 `已注销`（逻辑删除，保留历史借阅记录）。
> 2. 系统管理员的维护范围补齐为：借阅证自动发放 / 注销、增删管理员、增删图书标题与馆藏副本、**设置副本报废**、**调整预约队列**、**修改借阅与罚款规则**、**减免罚款**。
> 3. 图书管理员职责仅为：**借书 / 还书 / 查询借阅信息 / 查询全部借阅记录**，**无**图书标题、馆藏副本、管理员的维护权限。
### U1
先在系统中注册一个账号，然后系统为其生成一个借阅证，借阅证提供学生的姓名，系别和借阅证号。
没有有效期；状态~~（正常 / 挂失 / 冻结 / 注销）~~**仅有 `正常 / 已注销` 两态**（依 `R-12` 确认修订），该字段仅用于支撑 `D3` 的逻辑删除；不包含也不考虑挂失、冻结、补办流程。
### U2
必须由图书管理员在柜台代为办理
### U3
没有遗漏也不需要下列候选用例：读者挂失 / 注销借阅证、缴纳罚款、续借、催还通知、报表统计.需要缴纳超时罚款和遗失赔偿.「查询借阅信息」中读者只能查自己、管理员可查全部

> 措辞澄清（依 `R-3` 确认）：本句前半的「不需要缴纳罚款」指**不需要人工收费窗口 / 收银流程**，后半的「需要缴纳超时罚款」指**需要罚款结算这一业务能力**，二者不矛盾。最终用例命名为「**结算超期罚款**」与「**登记遗失赔偿**」，另由 `R-16` 补入「**读者自助缴纳罚款**」；「挂失 / 续借 / 催还通知 / 报表统计」确认不做。
### B1
系统管理员可修改借阅规则和罚款规则
### B2
以「读者类型」规定的期限为准,借出物类型和归还期限无关
### L1

专科生「最大可借数量3本」 「借阅期限15（天）」
本科生「最大可借数量5本」 「借阅期限30（天）」
研究生「最大可借数量7本」 「借阅期限30（天）」
博士生「最大可借数量10本」 「借阅期限60（天）」
教师  「最大可借数量15本」 「借阅期限60（天）」
### L2
借出物类型和归还期限无关,同一读者只能借同一「图书标题」的一本书,不能借同一标题的多本书
### L3
借阅前必须校验：有超期未还、有未缴罚款、已达数量上限
有这几种情况,不借
有未取预约提醒该读者
不支持续借,只能归还后在重新借阅
### F1
没有宽限期,所有读者超出借阅时间收取罚款，费率~~按一天5元~~**按借出物类型分档**（依 `R-1` 确认修订）：

| 借出物类型 | 宽限期（天） | 罚款费率（元/天） |
|---|---|---|
| 中文图书 | 0 | 2 |
| 外文图书 | 0 | 2.5 |
| 中文杂志 | 0 | 1 |
| 外文杂志 | 0 | 1.5 |
| 论文 | 0 | 3 |

罚款费率只与借出物类型有关，**不区分读者类型**（无教师减免）。
### F2
只有单读者封顶金额为50元,到达后不允许继续借阅,进入黑名单,直到缴清罚款。
计提时机~~归还时一次结算~~**每日定时计提**（依 `R-9` 确认修订）：定时任务对「在借且已超期」的记录按日累计罚款，使封顶与黑名单可在借阅过程中触发。
### F3
系统自动登记罚款,罚款未结清禁止继续借阅，超过封顶金额50后拉入黑名单。
处理方式（依 `R-3` / `R-16` 确认补全）：罚款记录状态为 `未缴 / 已缴`；**读者在系统内自助缴纳**本人罚款，**不需要管理员代办，也不要求在还书环节缴纳**；系统管理员可对单笔罚款**减免**（减至 0 视为结清）。缴清后解除「未结清」限制并移出黑名单。缴款为记账式结算，不接入真实支付网关（符合 Brief 第 8 节）。
### R1
只有某标题的**全部副本都已借出**时才允许预约
### R2
严格按预约时间 FIFO,但允许系统管理员手工调整队列
### R3
副本归还且有预约时，进入「预约保留」状态并暂停对外借出.保留3天.逾期未取顺延给下一位.不需要到馆通知（本项目不需要真实发邮件/短信,每位读者的预约数量上限为3.可主动取消
### P1
「维护图书标题信息」「维护馆藏副本」「维护管理员」
「修改罚款」属于系统管理员
计算罚款由系统自动计算
「查询全部借阅记录」分别属于图书管理员
系统管理员不可以借书
图书管理员不能修改自己的账号信息

> 权限矩阵补齐（依 `R-10` / `R-6` / `R-5` / `R-14` / `R-16` 确认；原三项「维护图书标题信息 / 维护馆藏副本 / 维护管理员」均归**系统管理员**）：
>
> | 事项 | 归属角色 |
> |---|---|
> | 维护图书标题信息 | 系统管理员 |
> | 维护馆藏副本 | 系统管理员 |
> | 设置副本报废 | 系统管理员 |
> | 维护管理员（增删） | 系统管理员 |
> | 修改借阅规则 / 罚款规则 | 系统管理员 |
> | 减免罚款（单笔） | 系统管理员 |
> | 调整预约队列 | 系统管理员 |
> | 修改管理员账号信息 | 系统管理员 |
> | 借书 / 还书 | 图书管理员 |
> | 查询全部借阅记录 | 图书管理员 |
> | 查询本人借阅信息 | 读者 |
> | 缴纳罚款（自助） | 读者 |
> | 预约 / 取消预约 | 读者 |
>
> 约束重申：**系统管理员不可借书**；**图书管理员不能修改自己的账号信息**；图书管理员**无**任何维护类权限。
### P2
需要审计日志
### P3
本地用户名 + 口令
要求数字+字母
口令强度为8位
不要求第三方登录
### D1
确认模型为「1 个图书标题 → N 个馆藏副本」，每个副本有唯一编号
副本的状态集合：`在馆 / 已借出 / 预约保留 / 遗失 / 报废`（依 `R-5` 的确认修订：**含报废、不含损坏**；**系统管理员**有权将副本置为「报废」）
### D2
中文/外文图书、中文/外文杂志、论文 —— 这是「图书标题」的属性,同一书的中文和外文版按两种书名来算
### D3
删除策略： 采用逻辑删除。读者注销或副本报废时，仅更改其状态字段，保留历史借阅记录，以保证数据可追溯性和统计报表的完整性。

数据规模预估： 基于课程作业定位，预计读者数在 5000 以内，副本数在 10000 以内，日均借还 300 次左右，累计流水在 10 万级别。

### T1
后端语言/框架Java + Spring Boot
数据库h2
形态是前后端分离的 Web 应用
### T2
1. **分层：** 是，要求「表现层 / 应用层 / 领域层 / 基础设施层」四层。
2. **DDD：** 不要求严格 DDD 聚合，但要求引入 Repository 概念（因推荐 Spring Data JPA），领域层需承载业务规则，不能用经典三层糊弄。
3. **API 契约：** 是，必须交付 `14-api-spec.md`，用 OpenAPI 形式。
4. **部署：** 单机可运行（H2 零配置启动；**运行 = 文件模式**，**测试 = 内存模式**，依 `R-8` 的双模式结论）
### Q1

1. **需要的测试层次：** 三层都要——**领域单元测试**（测 Policy/Service 业务规则）、**集成测试**（测 Repository 与数据库交互）、**API 接口测试**（测 Controller 端到端 HTTP 行为）。
2. “核心用例必须有自动化测试”，至少覆盖**借书、还书、预约、超期罚款、权限失败**五个场景。
3. **种子数据与测试专用数据库：** **必须有**。使用 **H2 内存数据库**（Java 技术栈首选）
### Q2
以全部自动化测试通过为准,必须端到端跑通
### E1
是优先纵向打通「借书 → 还书」核心链路（含罚款、预约的关键分支）,再要求其他十项
### E2
不需要
### E3
每次使用 Agent 生成 specs、UML、代码或测试，都必须记录在 ai-usage-log.md 中.截止时间充裕,无进度风险

---

## 复审（Review）

> 本节在人类完成 A1–E3 回答后由 AI 复审生成，**只登记待确认项，不修改上述任何回答**。
> 处理方式：人类逐条确认结果 → 回填到对应编号下 → 仍未确认的记入 `17-risk-analysis.md`。
> 复审日期：2026-09-21

### 严重程度汇总

| 编号 | 主题 | 类型 | 严重程度 | 状态 | 关联回答 |
|---|---|---|---|---|---|
| R-1 | 罚款规则未区分借出物类型 | 与 Brief 冲突 | ~~阻塞~~ | **已解决**（费率按借出物类型分档） | F1 / B2 / L2 |
| R-2 | 运行时数据库 MySQL 与 H2 不一致 | 回答内部矛盾 | ~~阻塞~~ | **已解决**（统一为 H2） | T1 / T2 / Q1 |
| R-3 | 「缴纳罚款」用例表述自相矛盾 | 回答内部矛盾 | 高 | **已解决**（无人工收费窗口；需「超期罚款结算」+「遗失赔偿」） | U3 / F3 |
| R-4 | 借阅/罚款规则存储在何处未回答 | 回答不完整 | 高 | **已解决**（配置驱动 + 数据库表） | B1 |
| R-5 | 副本状态集合缺少「损坏 / 报废」 | 回答内部矛盾 | 中 | **已解决**（五态：含报废、**不含损坏**；管理员可报废） | D1 / D3 / U3 |
| R-6 | 「修改罚款」权限语义不明 | 表述模糊 | 中 | **已解决**（①规则维护 + ②减免；缴纳归读者） | P1 / F2 / F3 |
| R-7 | 会话超时策略未回答 | 回答不完整 | 低 | **已解决**（30 分钟） | P3 |
| R-8 | **H2 的运行模式未定：内存 vs 文件** | 回答不完整 + 与 D3 冲突 | ~~阻塞~~ | **已解决**（双模式：运行 = 文件、测试 = 内存） | T1 / T2 / D3 |
| R-9 | 罚款计提时机自相矛盾 | 回答内部矛盾 | 高 | **已解决**（方案 2：每日定时计提） | F2 / F3 / L3 |
| R-10 | 权限矩阵只列事项、未分配角色 | 回答不完整 | 高 | **已解决**（三项归系统管理员；账号由系统管理员维护） | P1 / A2 |
| R-11 | 借阅证的生成主体冲突 | 回答内部矛盾 | 高 | **已解决**（方案 1：注册自动发证） | U1 / A2 |
| R-12 | 「借阅证无状态」与「逻辑删除」不可共存 | 回答内部矛盾 | 高 | **已解决**（方案 1：保留最小 `status`） | U1 / D3 |
| R-13 | 前端技术栈未定 | 回答不完整 | 中 | **已解决**（Vue 3 + Vite，SPA） | T1 / T2 |
| R-14 | 预约队列调整的权限归属不一致 | 回答内部矛盾 | 中 | **已解决**（归系统管理员，补入 P1） | R2 / A2 / P1 |
| R-15 | 「遗失赔偿」没有规则 | 需求缺口 | 中 | **已解决**（管理员人工录入金额 + 副本置遗失；复用结清路径） | U3 / F1-F3 |
| R-16 | 「罚款缴清」没有操作路径 | 需求缺口 | 中 | **已解决**（读者自助缴纳 + 管理员减免） | F2 / F3 / P1 / Brief §8 |
| R-17 | 口令哈希算法未回答 | 回答不完整 | 低 | **已解决**（BCrypt） | P3 |
| R-18 | 「最大可借数量」计数口径未定 | 表述模糊 | 低 | **已解决**（按在借副本数计） | L1 / L2 |
| R-19 | `E2`「不需要状态图」与副本状态机冲突 | 交付范围风险 | 低 | **已解决**（不新增 `.puml`，用文字状态迁移表） | E2 / D1 / R3 |

---

### R-1 罚款规则未区分借出物类型（阻塞）

**冲突双方**

- `00-project-brief.md` 第 6 节：「不同借出物类型具有不同超期规则和罚款规则。」
- `F1`：「没有宽限期，所有读者超出借阅时间按一天 5 元收取罚款。」
- 旁证：`B2`「借出物类型和归还期限无关」、`L2` 同义。

**后果**
中文图书 / 外文图书 / 中文杂志 / 外文杂志 / 论文 五类在期限与罚款上完全等价，「借出物类型」退化为**纯分类标签**；Brief 第 6 节需求落空，`05-domain-model.md` 中的类型层次失去业务动机。

**待确认（三选一）**

1. **接受简化**：修改 Brief 第 6 节为「借出物类型仅用于分类，不影响借阅期限与罚款」；
2. **保留差异**：给出 5 种类型各自的宽限期与费率（元/天），与读者类型组合成规则矩阵；
3. **部分差异**：仅区分「图书类」与「非图书类（杂志 / 论文）」两档。

**默认处理**：未确认时按方案 1 简化，并记入风险。

> **人类确认**：选择**方案 2「保留差异」**，罚款费率按**借出物类型**分档 —— **本项关闭**。
>
> | 借出物类型 | 罚款费率（元/天） | 宽限期 |
> |---|---|---|
> | 中文图书 | 2 | 0（无宽限期） |
> | 外文图书 | 2.5 | 0 |
> | 中文杂志 | 1 | 0 |
> | 外文杂志 | 1.5 | 0 |
> | 论文 | 3 | 0 |
>
> 连带修订（费率与原回答冲突，必须回改）：
> 1. `F1` 原文「所有读者超出借阅时间按一天 5 元」**作废**，改为「无宽限期；费率按借出物类型取上表，与读者类型无关」；
> 2. `F2` 的「单读者封顶 50 元 / 进黑名单」**不受影响**，仍为统一封顶；
> 3. `B2` / `L2` 的「借出物类型与归还期限无关」**仍然成立**（本项只影响罚款，不影响期限）；
> 4. `00-project-brief.md` 第 6 节「不同借出物类型具有不同超期规则和罚款规则」→ 改述为「不同借出物类型具有**不同罚款费率**；超期规则（无宽限期）与借阅期限不随类型变化」；
> 5. `13-database-design.md` 的 `fine_rule` 表以**借出物类型**为维度（5 行），`borrowing_rule` 仍以**读者类型**为维度。

---

### R-2 运行时数据库 MySQL 与 H2 不一致（**已解决**）

**原冲突**

- `T1`：「数据库 MySQL。」
- `T2`：「部署：单机可运行（推荐 H2 内存数据库零配置启动）。」
- `Q1`：「使用 H2 内存数据库」作为测试专用库。

**解决方式（人类确认：统一为 H2）**

人类已就地修改回答：

| 位置 | 修改前 | 修改后 |
|---|---|---|
| `T1` | `数据库MySQL` | `数据库h2` |
| `T2` | `单机可运行（推荐 H2 内存数据库零配置启动）` | `单机可运行（H2 零配置启动）`（运行模式后经 `R-8` 明确为「运行 = 文件、测试 = 内存」） |

现在 `T1` / `T2` / `Q1` 三处结论一致，均采用 **H2**。

**衍生结论**

- `13-database-design.md` 不再需要处理 MySQL / H2 **双方言**差异，原「`AUTO_INCREMENT`、`DATETIME` 精度、保留字」风险关闭；
- 但「统一为 H2」引出了**新的未决问题** —— H2 的**运行模式**（内存 / 文件）与 `D3` 的持久化要求冲突，见 **`R-8`**；本项与 R-8 必须一起读，否则会误以为数据库选型已无遗留问题。

> **人类确认**：`本项目统一采用 H2 数据库` —— 选型结论确认，**本项关闭**。
> **运行模式**（内存 / 文件）已由 `R-8` 单独定案为**双模式策略**（运行 = 文件模式、测试 = 内存模式）；为免出现两处口径不一致，该结论**仅登记在 `R-8` 处**，此处不重复。

---

### R-3 「缴纳罚款」用例表述自相矛盾（高）

**原文（`U3`）**

> 「没有遗漏也不需要下列候选用例：读者挂失 / 注销借阅证、**缴纳罚款**、续借、催还通知、报表统计.需要缴纳超时罚款和遗失赔偿.」

**问题**
前半句将「缴纳罚款」列为**不需要**，后半句又要求「需要缴纳超时罚款和遗失赔偿」，同一句中互斥。

**待确认**
推测本意为：**不需要人工缴费/收款流程**（与 `F3`「系统自动登记罚款」一致），但**需要「超期罚款结算」与「遗失赔偿」两个业务能力**。请确认该理解是否正确，或给出准确措辞。

**默认处理**：未确认时按上述推测执行，并在 `03-use-cases.md` 中把用例命名为「结算超期罚款」与「登记遗失赔偿」，避免与「缴纳罚款（收银）」混淆。

> **人类确认**：`按默认处理` —— 采纳上述推测，**本项关闭**。
> 补充：缴款动作由**读者在系统内自助完成**（见 `R-16` 定案），因此「人工收费窗口」确实不存在，与本结论不冲突。

---

### R-4 借阅/罚款规则存储在何处未回答（高）

**已答部分（`B1`）**：系统管理员可修改借阅规则和罚款规则。
**未答部分**：规则存放在数据库配置表中，还是代码内硬编码常量。

**推论**：既然管理员可**在运行时修改**，则必须是**配置驱动**，否则需要重新部署。据此登记为：

- `13-database-design.md` 需包含规则表（如 `borrowing_rule`：读者类型 × 最大可借数 × 借阅期限；`fine_rule`：封顶金额 × 日费率）；
- `00-project-brief.md` 第 6 节中「不同借出物类型不同规则」若按 R-1 简化，则规则表可退化为仅按**读者类型**建表。

**默认处理**：未确认时按「配置驱动 + 数据库表」执行。

> **人类确认**：`按默认处理` —— 采纳：规则以**数据库配置表**存储、管理员运行时可改，**本项关闭**。
> 连带：`13-database-design.md` 需含 `borrowing_rule`（读者类型 × 最大可借数 × 期限）；`fine_rule` 按 `R-1` 的确认改为**以借出物类型为维度**的费率表。

---

### R-5 副本状态集合缺少「损坏 / 报废」（中）

**冲突双方**

- `D1` 定义副本状态集合为：`在馆 / 已借出 / 预约保留 / 遗失`（无「损坏」「报废」）。
- `D3`：「读者注销或**副本报废**时，仅更改其状态字段，保留历史借阅记录。」
- `U3`：需要「遗失赔偿」用例。

**问题**
`D3` 引用了「副本报废」这一状态，但 `D1` 的状态集合中不存在该取值，属于状态机不完备。`U3` 的「遗失」赔偿已被 `D1` 覆盖，但「损坏」未定义。

**待确认**
副本状态集合是否修正为：`在馆 / 已借出 / 预约保留 / 遗失 / 损坏 / 报废`？其中「损坏」「报废」是否纳入本项目实现范围？

**默认处理**：未确认时按**六态**建模（含损坏、报废），并在 `06-domain-class-diagram.puml` 中显式标出状态枚举。

> **人类确认**：`加上报废但不要损坏` → **与上述默认处理不同，以本条为准**：副本状态集合定为**五态** —— `在馆 / 已借出 / 预约保留 / 遗失 / 报废`（**不含「损坏」**），**本项关闭**。
> 附加授权：**系统管理员**有权将副本置为「报废」。
> 连带修订：
> 1. `D1` 原文（本文件「人类回答」`D1` 段）已同步改为五态；
> 2. 「设置副本报废」需补入 `P1` 权限矩阵（归**系统管理员**）与 `03-use-cases.md` 用例清单；`P1` 本身另有 `R-10` 的补齐项；
> 3. `05-domain-model.md` / `06-domain-class-diagram.puml` 的状态枚举按**五态**生成；`遗失 → 报废` **不由系统自动触发**，需系统管理员另行执行「设置副本报废」（依 `R-15` 确认）；
> 4. 「损坏」不在本次范围：不出现在状态机、用例、数据库约束与测试用例中。

---

### R-6 「修改罚款」权限语义不明（中）

**原文（`P1`）**：「修改罚款」属于系统管理员；计算罚款由系统自动计算。

**问题**
`F2`/`F3` 已定「系统自动计算、单读者封顶 50 元、到达后进黑名单」，则「修改罚款」具体指以下哪一项：

1. 修改**罚款费率/封顶规则**（与 `B1` 的「可修改罚款规则」重复，应统一表述）；
2. 对单笔罚款记录做**减免 / 豁免**；
3. **登记已缴**（与 R-3 相关）。

**默认处理**：未确认时按 ①+② 执行（规则维护 + 单笔减免），不实现收款登记。

> **人类确认**：`就默认处理` —— 采纳 **①修改罚款规则 + ②对单笔罚款减免**（均归**系统管理员**），**本项关闭**。
> ③「登记已缴」**不设为管理员权限**；但缴纳动作本身由**读者自助**完成（见 `R-16`），管理员不代办收款 —— 两者不冲突。
> 连带：`P1` 需明确「**减免罚款** → 系统管理员」「**缴纳罚款** → 读者」。

---

### R-7 会话超时策略未回答（低）

**原文（`P3`）**：已答「本地用户名 + 口令」「数字 + 字母」「口令强度 8 位」「不要求第三方登录」；**未答会话超时时间**。

**默认处理**：未确认时取 30 分钟无操作自动登出，记入 `07-architecture.md`。

> **人类确认**：`默认处理` —— 采纳 **30 分钟**无操作自动登出，**本项关闭**。

---

### R-8 H2 的运行模式未定：内存 vs 文件（阻塞）

**冲突双方**

- `T2`：「部署：单机可运行（**H2 内存数据库**零配置启动）。」
- `Q1`：测试使用 H2 内存数据库。
- `D3`：「采用逻辑删除。读者注销或副本报废时，仅更改其状态字段，**保留历史借阅记录，以保证数据可追溯性和统计报表的完整性**。」规模：读者 5000、副本 10000、日均借还 300、累计流水 10 万。

**问题**
内存模式（`jdbc:h2:mem:`）的数据库随进程退出即销毁。若运行环境也使用内存模式，则 `D3` 要求保留的十万级历史流水、以及「统计报表」目标全部无法实现，逻辑删除也失去意义（删与不删都会被清空）。

**待确认**
是否按「同一 H2、按环境区分运行模式」处理：

| 场景 | 连接串 | 说明 |
|---|---|---|
| 运行 / 演示 / 实验验收 | `jdbc:h2:file:./data/library` | 文件模式，持久化，满足 D3 |
| 测试 / CI | `jdbc:h2:mem:testdb` | 内存模式，零污染，满足 Q1 |

**默认处理**：未确认时按上表执行，并在 `07-architecture.md` 中写明「数据库统一为 H2，仅运行模式按环境区分」——注意此结论与 R-2 的「统一为 H2」不冲突，R-2 解决的是**产品选型**，本项解决的是**运行模式**。

> **人类确认**：`必须满足 D3 的要求，用第二个方案`（即上表的双模式策略）—— **本项关闭**（原阻塞项解除）。
>
> | 环境 | 连接串 | 目的 |
> |---|---|---|
> | 生产 / 演示 / 实验验收 | `jdbc:h2:file:./data/library` | 文件模式，持久化，满足 `D3` 的十万级流水与统计报表 |
> | 开发 / 测试 / CI | `jdbc:h2:mem:library` | 内存模式，零污染、可重复运行，满足 `Q1` |
>
> 连带修订：
> 1. `07-architecture.md` 需写明**双模式连接串**（`spring.datasource.url` 按 profile 区分）、`ddl-auto` 与 schema / data 初始化脚本策略，以及文件模式下 `./data/` 的生成与 `.gitignore` 处理；
> 2. `T2` 原文的「H2 **内存**数据库零配置启动」已同步改述（见「人类回答」`T2` 第 4 点）；
> 3. `13-database-design.md` 以 H2 为唯一目标方言，无需双方言兼容层。

---

### R-9 罚款计提时机自相矛盾（高）

**冲突双方**

- `F2`：「…**归还时一次结算**。」
- `F3`：「系统**自动登记**罚款，罚款未结清禁止继续借阅，超过封顶金额 50 后拉入黑名单。」
- `L3`：借阅前校验「有超期未还、有未缴罚款、已达数量上限」。

**问题**
若罚款只在**归还时**结算，则在借未还期间罚款恒为 0，`F2` 的「单读者封顶 50 元 → 进入黑名单」**永远无法由「在借超期」触发**，黑名单机制实际被架空；兜底只剩 `L3` 的「有超期未还即拒绝借阅」，而 `L3` 并未设定金额阈值，与 `F2` 的 50 元封顶是两套不同规则。

**待确认（二选一）**

1. **仅归还时结算**（以 `F2` 为准）：罚款记录只在归还动作中产生；需确认「黑名单」仅由「已归还但未缴款」触发，且 `L3` 的「超期未还」作为**独立的**拒绝条件保留；
2. **每日定时计提**（以 `F3` 为准）：新增定时任务，对「在借且已超期」的记录按日累计罚款，使封顶与黑名单可在借阅过程中触发。

**默认处理**：未确认时按方案 2 执行，因为它能同时满足 `F2` 的封顶黑名单与 `F3` 的自动登记；代价是需引入定时任务组件，`07-architecture.md` 需增加相应说明。

> **人类确认**：`按默认处理` —— 采纳**方案 2「每日定时计提」**，**本项关闭**。
> 连带修订：
> 1. `F2` 原文「归还时一次结算」**作废**，改为「每日定时任务对『在借且已超期』的记录按日累计计提」；
> 2. 需引入定时任务（Spring `@Scheduled`）组件，`07-architecture.md` 需增加说明；
> 3. **计提与缴纳解耦**：罚款在**归还之前**即已入账，读者可随时自助缴纳，**不要求在还书环节缴费**（与 `R-16` 一致）；
> 4. 与 `R-16` 的组合效果：读者在借期间即可看到已计提罚款并**自助缴纳**，`F2` 的「50 元封顶 → 黑名单」可在借阅过程中触发。

---

### R-10 权限矩阵只列了事项、未分配角色（高）

**原文（`P1`）**

> 「维护图书标题信息」「维护馆藏副本」「维护管理员」

三项并列，**没有任何角色信息**；而其后的各项（修改罚款 / 计算罚款 / 查询全部借阅记录 / 系统管理员不可借书 / 图书管理员不可改自己账号）都写明了归属。

**问题**
权限矩阵是 `03-use-cases.md` 与鉴权设计的直接输入，缺归属无法落地。

**待确认**

1. 上述三项是否均归**系统管理员**？（按 `A2` 的职责清单推断应为系统管理员。）
2. 若如此，**图书管理员**的职责是否仅为：借书、还书、查询借阅信息（`A2`）+ 查询全部借阅记录（`P1`），确认其**无**「图书 / 标题 / 副本维护」权限？
3. `P1` 说「图书管理员不能修改自己的账号信息」，那么**由谁修改**？建议明确为「由系统管理员修改」，否则该规则无处执行。

**默认处理**：未确认时按「三项均属系统管理员；管理员账号信息由系统管理员维护」执行。

> **人类确认**：`对默认处理` —— 采纳，**本项关闭**：
> 1. 「维护图书标题信息 / 维护馆藏副本 / 维护管理员」**均归系统管理员**；
> 2. **图书管理员**职责仅为**借书 / 还书 / 查询借阅信息 / 查询全部借阅记录**，**无**图书、标题、副本维护权限；
> 3. 管理员账号信息**由系统管理员**修改。
> 连带：`P1` 还需按 `R-5` / `R-14` / `R-16` 补入「设置副本报废」「调整预约队列」「减免罚款」（系统管理员）与「自助缴纳罚款」（读者）。

---

### R-11 借阅证的生成主体冲突（高）

**冲突双方**

- `U1`：「先在系统中注册一个账号，然后**系统为其生成**一个借阅证，借阅证提供学生的姓名，系别和借阅证号。」
- `A2`：系统管理员的职责「主要包括**办理借阅证，删除借阅证**…」

**问题**
「系统自动生成」与「管理员办理」是两种不同的业务流：前者是注册动作的副作用，后者是一个独立的管理用例，二者不能同时成立。

**待确认（二选一）**

1. 账号注册成功后**自动**生成借阅证（无审核环节）；
2. 注册后**待审核**，由系统管理员「办理借阅证」后才生效 —— 则 `03-use-cases.md` 需新增该用例，且系统中存在「已注册未发证」的中间状态。

**默认处理**：未确认时按方案 1（自动生成）执行，并将 `A2` 中的「办理借阅证」理解为同一次注册在管理侧的等价表述。

> **人类确认**：`默认` —— 采纳**方案 1**：账号注册成功后**自动生成**借阅证（**无审核环节**，不存在「已注册未发证」的中间状态），**本项关闭**。
> 连带：`A2` 的「办理借阅证 / 删除借阅证」在实现上等价于**注册时自动发证**与**读者注销**（与 `R-12` 方案 1 的 `status` 字段对应）；`03-use-cases.md` 不新增「办理借阅证」用例。

---

### R-12 「借阅证无状态」与「逻辑删除」不可共存（高）

**冲突双方**

- `U1`：借阅证「**没有有效期和状态**（正常 / 挂失 / 冻结 / 注销），不包含也不考虑补办与注销流程」。
- `D3`：「采用逻辑删除。**读者注销**或副本报废时，仅更改其**状态字段**，保留历史借阅记录…」

**问题**
`D3` 的逻辑删除依赖「读者 / 借阅证的状态字段」与「注销」这一状态迁移，但 `U1` 明确声明该字段与该流程都不存在。结论：**当前逻辑删除策略没有可操作的落点**，读者表与借阅证表也无法表达「已注销」。

**待确认（二选一）**

1. **保留最小状态字段**：借阅证 / 读者增加 `status`（如 `正常 / 已注销`），**不做**挂失、冻结、补办的界面流程，仅保留字段以支撑逻辑删除 —— 需相应修订 `U1` 措辞（「没有状态」改为「状态仅有 `正常 / 已注销` 两态」）；
2. **放弃读者逻辑删除**：读者物理删除，仅对**副本**（报废）与**借阅记录**保留逻辑删除。

**默认处理**：未确认时按方案 1 执行，它同时满足 `D3` 的可追溯性要求，且未引入 `U1` 所排除的挂失 / 冻结 / 补办流程。

> **人类确认**：`默认` —— 采纳**方案 1**：读者 / 借阅证保留最小 `status` 字段（`正常 / 已注销`），**不做**挂失、冻结、补办流程，**本项关闭**。
> 连带：`U1` 措辞需修订为「借阅证无有效期；状态仅有 `正常 / 已注销` 两态；不考虑挂失、冻结与补办流程」，使 `D3` 的「读者注销 = 逻辑删除」获得落点。

---

### R-13 前端技术栈未定（中）

**原文（`T1`）**：「后端语言/框架 Java + Spring Boot」「数据库 h2」「形态是前后端分离的 Web 应用」——**未指定前端框架**。

**问题**
`07-architecture.md` 的表现层设计、`14-api-spec.md` 的调用方形态、以及最终可运行代码的交付物形态都取决于此。注意：若选择服务端渲染（如 Thymeleaf），则与「前后端分离」的表述相矛盾，必须二选一。

**待确认**：Vue 3 / React / 原生 HTML + JS？是否存在 Node.js 构建链？

**默认处理**：未确认时按 **Vue 3 + Vite** 执行，并在 `07-architecture.md` 中标注为假设。

> **人类确认**：`按默认处理` —— 前端技术栈定为 **Vue 3 + Vite**，**本项关闭**。
> 定案内容：
> 1. 前后端分离的 **SPA + JSON API**，**不采用** Thymeleaf 等服务端渲染（与 `T1`「形态是前后端分离的 Web 应用」一致）；
> 2. 存在 **Node.js 构建链**（`npm` / `vite build`），前端源码与构建产物 `dist/` 纳入交付物；
> 3. 开发期由 Vite dev server 代理 `/api` 至 Spring Boot；生产形态为 `dist/` 静态资源由后端托管。
> 连带：`07-architecture.md` 表现层章节、`14-api-spec.md` 调用方形态、`16-tasks.md` 前端脚手架任务、`17-risk-analysis.md` 登记「新增 Node.js 运行时依赖」。

---

### R-14 预约队列调整的权限归属不一致（中）

**冲突双方**

- `R2`：「严格按预约时间 FIFO，但允许**系统管理员**手工调整队列。」
- `A2`：系统管理员的职责清单（办理 / 删除借阅证、增删管理员、增删图书、增删标题信息）**不含**预约队列调整。
- `P1`：权限矩阵**未包含**「调整预约队列」这一项。

**问题**
同一项职责在 `R2` 与 `A2` / `P1` 中不一致，且权限矩阵缺少该条目。

**待确认**：队列调整归**系统管理员**还是**图书管理员**？

**默认处理**：未确认时按 `R2` 原文归**系统管理员**，并在 `P1` 权限矩阵中补记该条目（P1 本身也需按 R-10 一并补齐）。

> **人类确认**：`对默认处理` —— 采纳：预约队列调整归**系统管理员**，**本项关闭**。
> 连带：「调整预约队列」需补入 `P1` 权限矩阵与 `03-use-cases.md`（与 `R-10` 的 `P1` 补齐一并处理）。

---

### R-15 「遗失赔偿」没有规则（中）

**原文（`U3`）**：「…需要缴纳超时罚款和遗失赔偿。」

**问题**
`U3` 要求「遗失赔偿」这一业务能力，`D1` 也定义了副本的「遗失」状态，但 `F1`-`F3` **只定义了超期罚款**，完全未涉及：

- 赔偿金额如何确定（定价倍数 / 固定金额 / 人工录入）？
- 由谁认定「遗失」（读者申报 / 管理员登记）？
- 赔偿完成后副本状态如何流转（遗失 → 报废？遗失 → 找回后归馆？）

**待确认**：给出遗失赔偿规则，或将「遗失赔偿」在本次范围内显式降级为**仅登记、不定金额**。

**默认处理**：未确认时按「管理员登记赔偿金额 + 副本置为遗失」的最小实现，并在 `17-risk-analysis.md` 中记录为规则缺口。

> **人类确认**：`按默认处理` —— 采纳上述**最小实现**，**本项关闭**。
> 规则定稿（以此为准）：
> 1. **认定方**：由**图书管理员**在柜台受理读者申报后登记，系统**不自动判定**遗失；
> 2. **金额**：由**登记人人工录入**赔偿金额，系统**不做**定价倍数 / 固定金额计算 → 因此赔偿金额**不是** `fine_rule` 表的维度，而是赔偿记录上的输入字段；
> 3. **结清**：登记即产生一笔 `类型 = 赔偿` 的**未缴**款项，读者**自助缴纳**、系统管理员可**减免**（复用 `R-16` 的同一套状态与结清路径）；
> 4. **副本状态**：登记赔偿时副本由 `已借出` 置为 `遗失`；`遗失 → 报废` **不由系统自动触发**，需系统管理员另行执行「设置副本报废」（`R-5`）；
> 5. **找回流程不实现**：无「遗失 → 在馆」流转（`U3` 未提出，符合 `E2` 最小交付）；
> 6. **不参与封顶**：`F2` 的 50 元封顶与黑名单**仅适用于超期罚款**，赔偿金额**不计入**该封顶（避免一本高价书直接把读者拉黑）。
> 连带：`03-use-cases.md` 新增用例「登记遗失赔偿」（参与者：图书管理员，读者为申报方）；`13-database-design.md` 罚款表增加 `类型 = 超期 / 赔偿` 与金额、状态、结清时间字段；`02-requirements.md` 给出可测断言；`17-risk-analysis.md` 记录「赔偿金额人工录入，无自动定价规则」。

---

### R-16 「罚款缴清」没有操作路径（中）

**冲突双方**

- `F3`：「罚款未结清禁止继续借阅，超过封顶金额 50 后拉入黑名单。」
- `F2`：「…直到**缴清罚款**…」
- `00-project-brief.md` 第 8 节：「**暂不实现**：支付系统真实扣款。」
- `P1`：仅提到「修改罚款属于系统管理员」。

**问题**
系统需要判断「是否已缴清」，但**没有任何用例或角色负责把罚款置为已缴**。若不实现该动作，罚款记录将永久停留在「未结清」，读者一旦超期便**永久无法再借书**，黑名单也无法解除 —— 这是一个会导致系统不可用的闭环缺口。

**待确认**：由系统管理员（或图书管理员）执行「登记罚款已缴」，把罚款记录状态改为 `已缴`；该操作需补入 `03-use-cases.md` 与 `P1` 权限矩阵。

**默认处理**：未确认时按「系统管理员登记已缴」执行，与 `R-6` 的方案 ③ 合并处理。

> **人类确认**：`读者在系统上自己缴纳罚款`（另口头补充：`读者可以在系统自己缴纳，无需管理员帮助；系统管理员可以帮读者减免`）→ **替代上述默认处理，本项关闭**。
> 1. **读者自助缴纳**：读者登录后对本人未结清的罚款执行缴纳（**记账式结算**，不接入真实支付网关，仍符合 Brief 第 8 节「暂不实现支付系统真实扣款」）；
> 2. **系统管理员减免**：可对单笔罚款减免（与 `R-6` 的 ② 一致），减免至 0 视为结清；
> 3. 罚款记录增加状态字段（`未缴 / 已缴`），**自助缴纳**与**减免**均可使其结清 → `F2` 的「缴清后移出黑名单」获得可执行路径，**闭环缺口关闭**；
> 4. **与归还流程解耦**：缴纳是读者随时可发起的独立操作，**不绑定归还动作**；归还不因存在未缴罚款而被阻断（未缴罚款只影响 `L3` 的「继续借阅」校验）。
> 连带：`03-use-cases.md` 新增用例「读者自助缴纳罚款」（参与者：读者）；`P1` 补入「缴纳罚款 → 读者」「减免罚款 → 系统管理员」；`14-api-spec.md` 增加缴纳接口；`13-database-design.md` 的罚款表增加状态与结清时间字段。
> 备注：`U3` 原文「不需要缴纳罚款」与「需要缴纳超时罚款」的措辞矛盾由 `R-3` 一并处理，本项只解决**结清路径**。

---

### R-17 口令哈希算法未回答（低）

**原文（`P3`）**：已答「本地用户名 + 口令」「数字 + 字母」「口令强度 8 位」「不要求第三方登录」；**未答哈希算法**（会话超时另见 `R-7`）。

**默认处理**：未确认时按 Spring Security 默认的 **BCrypt**，并在 `07-architecture.md` 与 `13-database-design.md` 中体现（`password_hash` 字段长度按 60 设计）。

> **人类确认**：`默认` —— 采纳 **BCrypt**，**本项关闭**。
> 连带：`13-database-design.md` 的 `password_hash` 字段按 60 字符设计。

---

### R-18 「最大可借数量」的计数口径未定（低）

**冲突双方**

- `L1`：「专科生『最大可借数量 3 本』…教师『15 本』」
- `L2`：「同一读者**只能借同一「图书标题」的一本书**，不能借同一标题的多本书」

**问题**
在「同标题限 1 本」的约束下，`L1` 的「本数」是按**标题**计（3 个不同标题）还是按**副本**计，需要在接口校验与数据约束中明确。本例中两者结果相同，但表述差异会导致测试用例设计产生分歧。

**默认处理**：未确认时明确为**按在借记录数计**（每条在借记录对应一个不同标题），并在 `02-requirements.md` 中给出可测断言。

> **人类确认**：`同标题限一本且按副本计算` → **口径定为「按在借副本数计」**（每借出一个副本计 1），同时保留 `L2` 的「同一标题限借 1 个副本」约束，**本项关闭**。
> 说明：因「同标题限 1 副本」，「在借副本数」与「在借记录数」在数值上等价。
> 连带：`02-requirements.md` 需给出可测断言 —— `COUNT(未归还借阅记录) ≤ L1[读者类型].最大可借数`，且 `COUNT(同一标题的在借副本) ≤ 1`。

---

### R-19 `E2`「不需要状态图」与副本状态机的张力（低）

**冲突双方**

- `E2`：「不需要」补充状态图 / 活动图。
- `D1`：副本存在 `在馆 / 已借出 / 预约保留 / 遗失 / 报废` 状态集合（依 `R-5` 确认为五态）。
- `R3`：归还且有预约 → 进入「预约保留」并暂停外借 → 保留 3 天 → 逾期未取顺延下一位。

**问题**
副本与预约的状态迁移是本系统最复杂的业务逻辑，也是最需评审的部分。仅靠类图中的状态枚举难以表达「保留期到期后自动顺延」这类**时间驱动**迁移，评审时容易遗漏边界条件。

**建议（折中，与 `E2` 不冲突）**：不新增 `.puml` 文件，仍在 `06-domain-class-diagram.puml` 中以状态枚举 + 一张**文字状态迁移表**（现态 / 事件 / 次态 / 副作用）表达。

**默认处理**：未确认时按上述折中执行。

> **人类确认**：`按默认处理` —— 采纳折中方案，**本项关闭**。
> 定案内容：**不新增 `.puml` 文件**；在 `06-domain-class-diagram.puml` 内以**状态枚举**表达状态集合，并在 `05-domain-model.md` 中给出一张**文字状态迁移表**（列：现态 / 事件 / 次态 / 副作用）。迁移表**必须**覆盖以下**时间驱动**迁移，否则视为遗漏：
> 1. `预约保留` —（保留期 3 天到期且无人取）→ `在馆`，并**顺延**给队列中下一位预约者（`R3`）；
> 2. `预约保留` —（读者取书）→ `已借出`；
> 3. `已借出` —（登记遗失赔偿）→ `遗失`（`R-15`）；
> 4. `遗失` —（系统管理员执行报废）→ `报废`（`R-5`，**非自动**）。
> 连带：`05-domain-model.md` 含迁移表；`06-domain-class-diagram.puml` 只保留状态枚举；`17-risk-analysis.md` 记录「状态机以文字迁移表呈现，评审时需人工核对完整性」。

---

### 复审结论

**统计（截至 2026-09-21 人类确认后）**：复审项共 19 条（R-1 ~ R-19），其中 **已关闭 19 条**，**待人类确认 0 条**。

| 状态 | 编号 | 数量 |
|---|---|---|
| 已关闭 | R-1 ~ R-19（全部） | 19 |
| 待确认 | **无** | 0 |
| **阻塞** | 无（原阻塞项 `R-1`、`R-8` 均已关闭） | 0 |

其中 `R-13` / `R-15` / `R-19` 三条由人类确认「**按默认处理**」关闭 —— 这三条是**采纳 AI 的默认建议**而非独立裁决，其内容按「**已定案的决策**」执行，同时在 `17-risk-analysis.md` 中作为**已知约束 / 风险**登记备查。

**阻塞项**：**无**。`02-requirements.md` 及之后的文档链不再存在「不定则无法定稿」的前置项。

**人类确认产生的连带修订（回改清单）**

状态列含义：**已回改** = 已在 `01-clarifying-questions.md` / `00-project-brief.md` 内完成；**待下游** = 待 `02` 及之后的文档成文时逐条落实，不得静默跳过。

| 来源 | 需回改的位置 | 内容 | 状态 |
|---|---|---|---|
| R-1 | `F1`（「人类回答」段） | 「超出借阅时间按一天 5 元」→ 按借出物类型分档：中文图书 2 / 外文图书 2.5 / 中文杂志 1 / 外文杂志 1.5 / 论文 3（元/天）；仍**无宽限期**，不区分读者类型 | **已回改** |
| R-1 | `00-project-brief.md` §6 | 改述为「不同借出物类型具有**不同罚款费率**；超期规则与借阅期限不随类型变化」 | **已回改** |
| R-5 | `D1`（「人类回答」段） | 副本状态：`在馆 / 已借出 / 预约保留 / 遗失 / 报废`（不含损坏）；系统管理员可报废 | **已回改** |
| R-8 | `T2`（「人类回答」段） | 运行 = H2 **文件模式**；测试 = H2 **内存模式** | **已回改** |
| R-9 | `F2`（「人类回答」段） | 「归还时一次结算」→「**每日定时计提**」，使封顶与黑名单可在借阅过程中触发 | **已回改** |
| R-9 / R-16 | `F3`（「人类回答」段） | 补入罚款状态 `未缴 / 已缴`、**读者自助缴纳**、管理员减免；**缴纳与归还解耦，不要求在还书时缴费** | **已回改** |
| R-11 / R-12 | `U1`（「人类回答」段） | 借阅证状态改为「仅有 `正常 / 已注销` 两态」；不做挂失 / 冻结 / 补办；「办理借阅证」= 注册自动发证 | **已回改** |
| R-10 / R-11 / R-12 / R-14 / R-5 / R-16 | `A2`、`P1`（「人类回答」段） | 「办理借阅证」= 自动发证、「删除借阅证」= 读者注销；补齐权限矩阵（维护类 / 报废 / 队列调整 / 规则维护 / 减免归**系统管理员**，缴纳归**读者**） | **已回改** |
| R-3 / R-16 | `U3`（「人类回答」段） | 补注「不需要人工收费窗口」与「需要罚款结算能力」不矛盾；用例命名「结算超期罚款」「登记遗失赔偿」「读者自助缴纳罚款」 | **已回改** |
| R-11 / R-15 / R-16 | `00-project-brief.md` §4 | 补注业务清单：办理借阅证 = 自动发证；增补「缴纳罚款」「登记遗失赔偿」 | **已回改** |
| R-9 / R-16 | `10/11-sequence-*.puml`、`07-architecture.md` | 归还时序中**移除缴费步骤**（归还与缴纳解耦）；引入 `@Scheduled` 定时计提组件 | **待下游** |
| R-1 | `13-database-design.md` | `fine_rule` 以**借出物类型**为维度（5 行）；`borrowing_rule` 以**读者类型**为维度 | **待下游** |
| R-9 / R-16 | `13-database-design.md` / `14-api-spec.md` | 罚款表增加 `未缴 / 已缴` 状态与结清时间字段；新增读者自助缴纳接口与管理员减免接口 | **待下游** |
| R-5 / R-10 / R-14 / R-16 | `03-use-cases.md` | 补入「设置副本报废」「调整预约队列」「减免罚款」「读者自助缴纳罚款」四个用例及其参与者 | **待下游** |
| R-8 | `07-architecture.md` / `13-database-design.md` | 双模式连接串（按 profile 区分）、`ddl-auto` 与初始化脚本、文件模式 `./data/` 的生成与 `.gitignore`；以 H2 为唯一目标方言 | **待下游** |
| R-17 / R-7 / R-2 | `07-architecture.md` / `13-database-design.md` | BCrypt、`password_hash` 长度 60；会话超时 30 分钟；无需 MySQL/H2 双方言兼容层 | **待下游** |
| R-13 | `07-architecture.md` / `14-api-spec.md` / `16-tasks.md` | 前端定为 **Vue 3 + Vite**（SPA + JSON API，不采用服务端渲染）；新增 Node.js 构建链与 `dist/` 产物 | **待下游** |
| R-15 | `03-use-cases.md` / `13-database-design.md` / `02-requirements.md` / `17-risk-analysis.md` | 遗失赔偿＝**人工录入金额** + 副本置 `遗失`；复用 `R-16` 结清路径；**不计入 50 元封顶**；不实现找回流程 | **待下游** |
| R-19 | `05-domain-model.md` / `06-domain-class-diagram.puml` | 状态迁移以**文字迁移表**表达（不新增 `.puml`），须覆盖预约保留到期顺延等**时间驱动**迁移 | **待下游** |

**门槛判断**
无阻塞项、**无待确认项**，文档链可推进。`R-13` / `R-15` / `R-19` 均已由人类确认「按默认处理」并关闭，其内容按**已定案决策**执行（不再标注为待定 Assumption）；默认处理引入的新增依赖（Node.js 构建链、赔偿金额人工录入、状态机以文字表呈现）需在 `17-risk-analysis.md` 中作为**已知约束 / 风险**登记。

**待办**

1. ~~人类确认剩余 3 条~~ —— `R-13` / `R-15` / `R-19` 已按默认处理确认，**未确认项清零**，`16-tasks.md` 的前置门槛已满足；
2. 连带修订回改：上表状态为「**已回改**」的 10 行**已完成**（`F1` / `F2` / `F3` / `U1` / `U3` / `A2` / `P1` / `D1` / `T2`，以及 `00-project-brief.md` §4、§6）；状态为「**待下游**」的 9 行在 `02`~`17` 文档成文时逐条落实，其中「**归还与缴纳解耦**」「**赔偿金额人工录入**」「**前端 Vue 3 + Vite**」「**状态迁移表落在 `05`**」属硬性要求，不得静默省略；
3. `02-requirements.md` 的输入已齐备，可直接成文。